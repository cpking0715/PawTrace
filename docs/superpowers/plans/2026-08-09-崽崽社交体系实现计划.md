# 崽崽社交体系实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 PawTrace 纯前端 Demo 实现崽崽社交体系：碰个爪交玩伴、崽崽圈、地图玩伴标记、崽崽战报/手账、撒欢地搜索+宝藏标记。

**Architecture:** 延续现有单页多 `.page` 架构（无路由、无框架、无后端），所有新页面/浮层追加到 `demo/index.html`，逻辑追加到 `demo/app.js`，样式追加到 `demo/style.css`。数据为内存预置虚拟数据，刷新即重置。

**Tech Stack:** 原生 HTML/CSS/JS + Leaflet 1.9.4（地图/轨迹）+ 现有「清晨公园」设计令牌（`style.css :root`）。

**设计文档:** `docs/superpowers/specs/2026-08-09-崽崽社交体系设计.md`（实现必须与该文档一致，命名严格使用崽崽系）。

**环境与约定:**
- 本地预览：`python -m http.server 8090 --directory demo`（后台运行），浏览器访问 `http://localhost:8090/?v=N`（每次验证递增 N 破缓存），手机视口 390x844
- 验证方式：Chrome DevTools MCP（真实浏览器 E2E，本 Demo 无单测框架）。每个任务先人工/MCP 确认现状（红），实现后再验证（绿）
- 提交约定：不修改 git config，用环境变量注入身份：

```powershell
$env:GIT_AUTHOR_NAME='cpking0715'; $env:GIT_AUTHOR_EMAIL='cpking0715@users.noreply.github.com'; $env:GIT_COMMITTER_NAME='cpking0715'; $env:GIT_COMMITTER_EMAIL='cpking0715@users.noreply.github.com'; git add <files>; git commit -m "<msg>"
```

- 分支：直接在 master 开发（延续本仓库既有工作方式，用户已全程确认）

---

## 文件结构

| 文件 | 职责（本轮改动） |
|------|------|
| `demo/index.html` | 新增 6 个页面骨架（pageCircle/pageFriends/pageFriendDetail/pageSettings/pageWalkSummary/pageWalkRecords）+ bumpOverlay 浮层 + 发布/通用确认弹窗 + 好友狗信息卡；Tab 4→5；「崽崽家」页改名与菜单落地；撒欢地页加搜索框；地图页加碰个爪按钮 |
| `demo/app.js` | 新增数据模型（dogFriendsPool/myFriends/myLocationPrivacy/circlePosts/walkRecords/spotsData）、碰个爪流程、玩伴管理、崽崽在哪、地图玩伴/陌生汪标记、轨迹采样、崽崽战报/手账、崽崽圈渲染与发布、撒欢地搜索+宝藏渲染 |
| `demo/style.css` | 所有新组件样式与动画（弹跳/光环/雷达/拍爪等），追加到文件尾部 |
| `tests/test-cases.md` | 追加 TC-31+ 用例（模块 F~L） |
| `tests/test-results.md` | E2E 执行结果 |

**关键现有锚点**（实现时需对齐）：
- Tab 切换：`switchTab(page)` 与 `pageMap` 映射（app.js L242-275）
- 子页面模式：`openDogsPage()` 隐藏 tabBar、`.page.active` 切换（app.js L706-711）
- 遛狗模拟移动：`walkMoveTimer` 4 秒定时器（app.js L563-572）——轨迹采样挂这里
- 结束遛狗：`confirmEndWalk()`（app.js L628-652）——改为跳战报页
- 地点渲染：`initPlacesPage()` + `PLACES_DATA`（app.js L18-23, L662-684）
- 地图点击收卡：`map.on('click', ...)`（app.js L103）需追加收起好友卡

---

### Task 1: 导航骨架 —— 5 Tab + 命名改造 + 新页面骨架 + 子页面导航助手

**Files:**
- Modify: `demo/index.html`（Tab 栏、pageProfile、pagePlaces 头部、新页面骨架、碰个爪按钮、好友狗信息卡）
- Modify: `demo/app.js`（switchTab pageMap、initProfilePage 菜单绑定、openSubPage/closeSubPage 助手）
- Modify: `demo/style.css`（5 Tab 微调、新页面骨架样式占位）

- [ ] **Step 1: 确认现状（红）**

浏览器打开 `http://localhost:8090/?v=1`，确认：底部 4 个 Tab（地图/地点/遛狗/我的）；「我的」页称号为「遛狗达人」，「遛狗记录」「设置」菜单点击无反应。

- [ ] **Step 2: Tab 栏 4→5（`demo/index.html`）**

把原 4 个 tab-item 整体替换为（遛弯保持居中 `tab-walking`，崽崽圈插在崽崽家前）：

```html
      <div class="tab-bar" id="tabBar">
        <div class="tab-item active" data-page="map">
          <div class="tab-icon">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M9 20l-5.447-2.724A2 2 0 0 1 2 15.382V4.618a2 2 0 0 1 1.553-1.946l5.447 1.338 6-1.338 5.447 1.338A2 2 0 0 1 22 5.946v10.664a2 2 0 0 1-1.553 1.946L15 20l-3-1z"/></svg>
          </div>
          <div class="tab-label">地图</div>
        </div>
        <div class="tab-item" data-page="places">
          <div class="tab-icon">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M21 10c0 7-9 13-9 13S3 17 3 10a9 9 0 1 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
          </div>
          <div class="tab-label">撒欢地</div>
        </div>
        <div class="tab-item tab-walking" data-page="walking">
          <div class="tab-icon">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"><path d="M10 5.172C10 3.782 8.884 2.5 7.5 2.5S5 3.782 5 5.172C5 6.562 7.5 9 7.5 9s2.5-2.438 2.5-3.828Z"/><path d="M14.5 5.172c0-1.39 1.116-2.672 2.5-2.672s2.5 1.282 2.5 2.672C19.5 6.562 17 9 17 9s-2.5-2.438-2.5-3.828Z"/><path d="M12 22c-4 0-7.5-3-7.5-7.5 0-2 1-3.5 2.5-4.5"/><path d="M12 22c4 0 7.5-3 7.5-7.5 0-2-1-3.5-2.5-4.5"/><path d="M12 22V9"/></svg>
          </div>
          <div class="tab-label">遛弯</div>
        </div>
        <div class="tab-item" data-page="circle">
          <div class="tab-icon">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"><circle cx="8" cy="9" r="3.4"/><circle cx="16" cy="9" r="3.4"/><path d="M4.6 17.5c.8-1.8 2.6-2.9 3.4-2.9s2.6 1.1 3.4 2.9"/><path d="M12.6 17.5c.8-1.8 2.6-2.9 3.4-2.9s2.6 1.1 3.4 2.9"/></svg>
          </div>
          <div class="tab-label">崽崽圈</div>
        </div>
        <div class="tab-item" data-page="profile">
          <div class="tab-icon">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"><path d="M10 5.172C10 3.782 8.884 2.5 7.5 2.5S5 3.782 5 5.172C5 6.562 7.5 9 7.5 9s2.5-2.438 2.5-3.828Z"/><path d="M14.5 5.172c0-1.39 1.116-2.672 2.5-2.672s2.5 1.282 2.5 2.672C19.5 6.562 17 9 17 9s-2.5-2.438-2.5-3.828Z"/><path d="M12 22c-4 0-7.5-3-7.5-7.5 0-2 1-3.5 2.5-4.5"/><path d="M12 22c4 0 7.5-3 7.5-7.5 0-2-1-3.5-2.5-4.5"/></svg>
          </div>
          <div class="tab-label">崽崽家</div>
        </div>
      </div>
```

- [ ] **Step 3: 「崽崽家」页改名与菜单落地（`demo/index.html` pageProfile 区）**

3 处修改：
1. `<div class="profile-name">遛狗达人</div>` → `<div class="profile-name">崽崽家长</div>`
2. 「我的狗狗」统计与菜单：统计 label `我的狗狗` 数字包 `<span id="profileDogCount">2</span>`；菜单项 `id="menuMyDogs"` 文案改「家里崽崽」，badge 改 `<span class="menu-badge" id="menuMyDogsBadge">2只</span>`
3. 菜单重组（第一个 menu-group 替换为）：

```html
            <div class="menu-group">
              <div class="menu-item" id="menuMyDogs">
                <span class="menu-icon">
                  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#6BA3BE" stroke-width="2" stroke-linecap="round"><path d="M10 5.172C10 3.782 8.884 2.5 7.5 2.5S5 3.782 5 5.172C5 6.562 7.5 9 7.5 9s2.5-2.438 2.5-3.828Z"/><path d="M14.5 5.172c0-1.39 1.116-2.672 2.5-2.672s2.5 1.282 2.5 2.672C19.5 6.562 17 9 17 9s-2.5-2.438-2.5-3.828Z"/><path d="M12 22c-4 0-7.5-3-7.5-7.5 0-2 1-3.5 2.5-4.5"/><path d="M12 22c4 0 7.5-3 7.5-7.5 0-2-1-3.5-2.5-4.5"/></svg>
                </span>
                <span class="menu-text">家里崽崽</span>
                <span class="menu-badge" id="menuMyDogsBadge">2只</span>
                <span class="menu-arrow"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#B0B0B0" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg></span>
              </div>
              <div class="menu-item" id="menuFriends">
                <span class="menu-icon">🐾</span>
                <span class="menu-text">玩伴</span>
                <span class="menu-badge" id="menuFriendsBadge">0位</span>
                <span class="menu-arrow"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#B0B0B0" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg></span>
              </div>
              <div class="menu-item" id="menuWalkRecords">
                <span class="menu-icon">
                  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#6BA3BE" stroke-width="2" stroke-linecap="round"><path d="M12 20V10"/><path d="M18 20V4"/><path d="M6 20v-4"/></svg>
                </span>
                <span class="menu-text">崽崽手账</span>
                <span class="menu-arrow"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#B0B0B0" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg></span>
              </div>
            </div>
```

第二个 menu-group 的「设置」项加 id 并改名：`<div class="menu-item">` → `<div class="menu-item" id="menuSettings">`，文案「设置」→「小窝设置」。

- [ ] **Step 4: 新增页面骨架（`demo/index.html`，插在 pageDogEdit 之后、`</div><!-- /page-container -->` 之前）**

```html
        <!-- ===== 崽崽圈页（Tab） ===== -->
        <div class="page page-circle" id="pageCircle">
          <div class="page-header">
            <h1 class="page-title">🐶 崽崽圈</h1>
            <button class="btn-publish" id="btnPublishPost">晒崽崽</button>
          </div>
          <div class="circle-timeline" id="circleTimeline"></div>
        </div>

        <!-- ===== 玩伴列表页 ===== -->
        <div class="page page-friends" id="pageFriends">
          <div class="page-header">
            <button class="back-btn" id="btnBackFromFriends">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>
              返回
            </button>
            <h1 class="page-title">🐾 玩伴 <span class="friends-count" id="friendsCount">(0)</span></h1>
            <button class="btn-publish" id="btnBumpFromFriends">碰个爪</button>
          </div>
          <div class="friends-list" id="friendsList"></div>
        </div>

        <!-- ===== ta 家崽崽（玩伴狗狗档案） ===== -->
        <div class="page page-friend-detail" id="pageFriendDetail">
          <div class="page-header">
            <button class="back-btn" id="btnBackFromFriendDetail">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>
              返回
            </button>
            <h1 class="page-title" id="friendDetailTitle">ta 家崽崽</h1>
          </div>
          <div class="friend-detail-owner" id="friendDetailOwner"></div>
          <div class="friend-dogs-list" id="friendDogsList"></div>
        </div>

        <!-- ===== 小窝设置页 ===== -->
        <div class="page page-settings" id="pageSettings">
          <div class="page-header">
            <button class="back-btn" id="btnBackFromSettings">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>
              返回
            </button>
            <h1 class="page-title">小窝设置</h1>
          </div>
          <div class="settings-body">
            <div class="settings-section-title">崽崽在哪 <span class="settings-sub">我家崽崽的行踪被谁看到</span></div>
            <div class="privacy-option active" data-mode="friends">
              <div class="privacy-radio"></div>
              <div class="privacy-info">
                <div class="privacy-name">仅玩伴可见</div>
                <div class="privacy-desc">只有碰过爪的玩伴能看到我家崽崽的位置（推荐）</div>
              </div>
            </div>
            <div class="privacy-option" data-mode="everyone">
              <div class="privacy-radio"></div>
              <div class="privacy-info">
                <div class="privacy-name">所有汪星可见</div>
                <div class="privacy-desc">附近所有崽崽家长都能看到我家崽崽的位置</div>
              </div>
            </div>
            <div class="privacy-option" data-mode="off">
              <div class="privacy-radio"></div>
              <div class="privacy-info">
                <div class="privacy-name">关闭行踪</div>
                <div class="privacy-desc">谁都看不到我家崽崽的位置，安心宅家</div>
              </div>
            </div>
            <div class="settings-note" id="privacyOffNote" style="display:none;">行踪已关闭～谁也看不到我家崽崽在哪 🏠</div>
            <div class="settings-divider"></div>
            <div class="settings-link" id="btnGoPerFriendPrivacy">单独管理：对某位玩伴隐藏行踪 ›</div>
          </div>
        </div>

        <!-- ===== 崽崽战报页 ===== -->
        <div class="page page-walk-summary" id="pageWalkSummary">
          <div class="page-header">
            <h1 class="page-title">🏅 崽崽战报</h1>
          </div>
          <div class="summary-body">
            <div class="summary-map" id="summaryMap"></div>
            <div class="summary-stats">
              <div class="summary-stat"><div class="summary-stat-num" id="summaryDuration">00:00</div><div class="summary-stat-label">遛弯时长</div></div>
              <div class="summary-stat"><div class="summary-stat-num" id="summaryDistance">0m</div><div class="summary-stat-label">移动距离</div></div>
              <div class="summary-stat"><div class="summary-stat-num" id="summaryCalories">0</div><div class="summary-stat-label">消耗大卡</div></div>
            </div>
            <div class="summary-dogs" id="summaryDogs"></div>
            <div class="summary-actions">
              <button class="btn-confirm" id="btnPublishWalk">晒到崽崽圈</button>
              <button class="btn-reset" id="btnSummaryDone">返回地图</button>
            </div>
          </div>
        </div>

        <!-- ===== 崽崽手账页 ===== -->
        <div class="page page-walk-records" id="pageWalkRecords">
          <div class="page-header">
            <button class="back-btn" id="btnBackFromRecords">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>
              返回
            </button>
            <h1 class="page-title">📒 崽崽手账</h1>
          </div>
          <div class="records-list" id="recordsList"></div>
        </div>

        <!-- ===== 好友的崽崽信息卡（地图底部） ===== -->
        <div class="spot-card dog-info-card" id="friendDogCard">
          <div class="spot-card-handle"></div>
          <div class="dog-info-body">
            <div class="dog-info-head">
              <div class="dog-info-avatar" id="friendDogAvatar">🐕</div>
              <div class="dog-info-head-right">
                <div class="dog-info-name"><span id="friendDogName">崽崽</span> <span class="friend-badge" id="friendDogBadge">玩伴</span></div>
                <div class="dog-info-breed" id="friendDogBreed">-</div>
              </div>
            </div>
            <div class="dog-info-row" id="friendDogTagsRow">
              <span class="dog-info-label">脾气</span>
              <span class="dog-info-tags" id="friendDogTags"></span>
            </div>
            <div class="dog-info-row" id="friendDogLikesRow">
              <span class="dog-info-label">喜好</span>
              <span class="dog-info-tags" id="friendDogLikes"></span>
            </div>
            <button class="btn-confirm" id="btnFriendDogBump" style="display:none;">碰碰爪交个玩伴</button>
          </div>
        </div>
```

注意：`friendDogCard` 放在 `pageMap` 内（与 `dogInfoCard` 同级，跟随后者之后），保证只在地图页显示。

- [ ] **Step 5: 碰个爪浮层与弹窗（`demo/index.html`，插在 filterModal 之后、`</div><!-- /phone-screen -->` 之前）**

```html
      <!-- 碰个爪全屏浮层 -->
      <div class="bump-overlay" id="bumpOverlay">
        <button class="bump-close" id="btnBumpClose">✕</button>
        <div class="bump-stage stage-approach" id="bumpStageApproach">
          <div class="bump-paw paw-left">🐾</div>
          <div class="bump-paw paw-right">🐾</div>
          <div class="bump-tip">把手机靠近，感受附近的崽崽…</div>
        </div>
        <div class="bump-stage stage-radar" id="bumpStageRadar" style="display:none;">
          <div class="bump-radar"><div class="bump-radar-ring"></div><div class="bump-radar-ring"></div><div class="bump-radar-ring"></div><div class="bump-radar-core">🐶</div></div>
          <div class="bump-tip">正在感受附近的崽崽…</div>
        </div>
        <div class="bump-stage stage-result" id="bumpStageResult" style="display:none;">
          <div class="bump-found-tip">✨ 发现一位崽崽家长 ✨</div>
          <div class="bump-candidate-card">
            <div class="bump-candidate-avatar" id="bumpCandidateAvatar">🧑</div>
            <div class="bump-candidate-name" id="bumpCandidateName">-</div>
            <div class="bump-candidate-dist" id="bumpCandidateDist">就在附近</div>
            <div class="bump-candidate-dogs" id="bumpCandidateDogs"></div>
          </div>
          <button class="btn-confirm bump-confirm-btn" id="btnBumpConfirm">碰个爪，交玩伴 🐾</button>
        </div>
        <div class="bump-stage stage-success" id="bumpStageSuccess" style="display:none;">
          <div class="bump-success-icon">🐾</div>
          <div class="bump-success-paws" id="bumpSuccessPaws"></div>
          <div class="bump-success-tip">碰爪成功，已是玩伴～</div>
          <div class="bump-success-actions">
            <button class="btn-confirm" id="btnBumpViewDogs">看看 ta 家崽崽</button>
            <button class="btn-reset" id="btnBumpAgain">继续碰碰</button>
          </div>
        </div>
        <div class="bump-stage stage-empty" id="bumpStageEmpty" style="display:none;">
          <div class="bump-empty-icon">🌙</div>
          <div class="bump-tip">附近暂时没有出来遛弯的玩伴，去别处逛逛吧～</div>
          <button class="btn-reset" id="btnBumpEmptyClose">好的</button>
        </div>
      </div>

      <!-- 晒崽崽发布弹窗 -->
      <div class="modal-overlay" id="publishPostModal">
        <div class="modal-sheet">
          <div class="modal-handle"></div>
          <div class="modal-header">
            <h2>晒崽崽</h2>
            <button class="modal-close" data-close="publishPostModal">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
            </button>
          </div>
          <div class="modal-body">
            <div class="filter-section">
              <div class="filter-title">选崽崽</div>
              <div class="publish-dog-row" id="publishDogRow"></div>
            </div>
            <div class="filter-section">
              <div class="filter-title">封面风格</div>
              <div class="publish-cover-row" id="publishCoverRow"></div>
            </div>
            <div class="form-group">
              <textarea class="form-input" id="publishText" rows="3" placeholder="说点什么吧～"></textarea>
            </div>
          </div>
          <div class="modal-footer">
            <button class="btn-confirm" id="btnPublishConfirm" style="width:100%;">发布</button>
          </div>
        </div>
      </div>

      <!-- 通用确认弹窗（删除玩伴等） -->
      <div class="modal-overlay" id="confirmModal">
        <div class="modal-sheet modal-end-walk">
          <div class="modal-handle"></div>
          <div class="modal-body" style="text-align:center;padding:24px 16px 8px;">
            <div id="confirmText" style="font-size:15px;color:var(--c-text);">确认吗？</div>
          </div>
          <div class="modal-footer">
            <button class="btn-reset" id="btnConfirmCancel">取消</button>
            <button class="btn-confirm" id="btnConfirmOk">确认</button>
          </div>
        </div>
      </div>
```

- [ ] **Step 6: 地图页碰个爪按钮 + 撒欢地搜索框（`demo/index.html`）**

1. `map-top-right` 内、`btnFilter` 之前插入：

```html
            <button class="map-btn btn-bump" id="btnBump" title="碰个爪">🐾</button>
```

2. pagePlaces 标题「狗狗友好地点」→「撒欢地」，并在 `</div>(page-header)` 后、`placesList` 前插入：

```html
          <div class="places-search-wrap">
            <input type="text" class="places-search" id="placesSearch" placeholder="搜索撒欢地（名称/特色）" />
          </div>
```

- [ ] **Step 7: JS 导航逻辑（`demo/app.js`）**

1. `switchTab` 的 pageMap 改为：

```js
  const pageMap = { map: 'pageMap', places: 'pagePlaces', walking: 'pageWalking', circle: 'pageCircle', profile: 'pageProfile' };
```

2. 在 `switchTab` 中 `if (page === 'walking') {...}` 之后追加：

```js
  if (page === 'circle') {
    renderCircleTimeline();
  }
```

3. 新增子页面导航助手（放在 `openDogsPage` 附近）：

```js
// ===== Sub-page Navigation（隐藏 TabBar 的全屏子页） =====
function openSubPage(pageId) {
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  document.getElementById(pageId).classList.add('active');
  document.getElementById('tabBar').style.display = 'none';
}

function closeSubPage(pageId) {
  document.getElementById(pageId).classList.remove('active');
  document.getElementById('tabBar').style.display = '';
  switchTab(currentTab); // 恢复当前 Tab 页
}
```

4. `openDogsPage` 重写为复用助手：

```js
function openDogsPage() {
  openSubPage('pageDogs');
  renderDogsList();
}
```

并把 `initDogsPage` 中返回逻辑改为 `closeSubPage('pageDogs');`（其内部 `switchTab(currentTab)` 会恢复 pageProfile 的 active 与 tabBar）。同理 `initDogEditPage` 的返回按钮保持（编辑页返回狗列表页，不涉及 tabBar）。

5. `initProfilePage` 追加菜单绑定：

```js
function initProfilePage() {
  document.getElementById('menuMyDogs').addEventListener('click', () => {
    openDogsPage();
  });
  document.getElementById('menuFriends').addEventListener('click', () => {
    renderFriendsList();
    openSubPage('pageFriends');
  });
  document.getElementById('menuWalkRecords').addEventListener('click', () => {
    renderWalkRecords();
    openSubPage('pageWalkRecords');
  });
  document.getElementById('menuSettings').addEventListener('click', () => {
    renderSettingsPrivacy();
    openSubPage('pageSettings');
  });
}
```

（`renderFriendsList/renderWalkRecords/renderSettingsPrivacy` 在后续任务实现；本任务先建绑定，若担心报错可临时注释，但推荐按顺序完成后续任务后整体验证。）

- [ ] **Step 8: 骨架样式（`demo/style.css` 文件尾部追加）**

```css
/* ===== Task 1: 骨架样式 ===== */
.btn-bump { font-size: 18px; line-height: 1; }
.places-search-wrap { padding: 0 var(--sp-lg) var(--sp-sm); }
.places-search {
  width: 100%; box-sizing: border-box; padding: 10px 16px;
  border: 1px solid var(--c-border); border-radius: var(--r-pill);
  background: var(--c-surface); font-size: var(--fs-body); color: var(--c-text);
  outline: none;
}
.places-search:focus { border-color: var(--c-primary); box-shadow: 0 0 0 3px rgba(107,163,190,0.12); }
.btn-publish {
  padding: 6px 14px; border: none; border-radius: var(--r-pill);
  background: var(--c-primary-gradient); color: #fff;
  font-size: var(--fs-caption); font-weight: 600; box-shadow: var(--shadow-primary);
}
.btn-publish:active { transform: scale(0.95); }
.friends-count { font-size: var(--fs-caption); color: var(--c-text-tertiary); font-weight: 400; }
.menu-icon { display: flex; align-items: center; justify-content: center; }
```

- [ ] **Step 9: 验证（绿）**

`http://localhost:8090/?v=2` 逐项确认：
- 5 个 Tab：地图/撒欢地/遛弯/崽崽圈/崽崽家，切换高亮正常，遛弯居中凸出样式不破坏
- 崽崽圈 Tab 可见空页面骨架（标题+晒崽崽按钮，时间线为空属预期）
- 崽崽家页：称号「崽崽家长」；菜单「家里崽崽(2只)/玩伴(0位)/崽崽手账/小窝设置」可点（后续任务实现内容，不报错即可）
- 撒欢地页有搜索框；地图页右上出现 🐾 按钮
- 控制台无 JS 错误（后续任务的 render 函数未实现前若报错，说明 Step 7 绑定顺序有误，修正后再验）

- [ ] **Step 10: 提交**

```powershell
$env:GIT_AUTHOR_NAME='cpking0715'; $env:GIT_AUTHOR_EMAIL='cpking0715@users.noreply.github.com'; $env:GIT_COMMITTER_NAME='cpking0715'; $env:GIT_COMMITTER_EMAIL='cpking0715@users.noreply.github.com'; git add demo/; git commit -m "feat: 崽崽系导航骨架（5 Tab + 命名改造 + 新页面/浮层骨架）"
```

---

### Task 2: 数据模型 + 撒欢地搜索/宝藏标记

**Files:**
- Modify: `demo/app.js`（spotsData 替换 PLACES_DATA、dogFriendsPool/myFriends/myLocationPrivacy/circlePosts/walkRecords、renderPlaces/initPlacesPage）
- Modify: `demo/style.css`（宝藏卡片样式）

- [ ] **Step 1: 替换地点数据与新增社交数据（`demo/app.js`）**

删除 `PLACES_DATA`（L18-23），在同位置写入：

```js
// 撒欢地地点（单一列表，含宝藏标记；lat/lng 用于地图 flyTo）
let spotsData = [
  { id: 1, name: '人民公园', dist: '0.8km', note: '有专门的狗狗活动区', stars: 4.8, lat: 30.6586, lng: 104.0553,
    isTreasure: true, treasureReason: '专属狗狗活动区 + 饮水点，崽崽社交天花板', hot: '本周 128 只崽崽来过', tags: ['狗狗活动区', '可脱绳', '有水源'] },
  { id: 2, name: '浣花溪公园', dist: '1.2km', note: '傍晚狗多，有大草坪', stars: 4.5, lat: 30.6436, lng: 104.0353,
    isTreasure: true, treasureReason: '超大草坪 + 林荫道，夏天遛弯不晒', hot: '本周 96 只崽崽来过', tags: ['大草坪', '林荫道', '傍晚狗多'] },
  { id: 3, name: '锦城湖公园', dist: '2.5km', note: '周末人多，建议工作日去', stars: 4.0, lat: 30.5836, lng: 104.0653,
    isTreasure: false, tags: ['湖畔步道'] },
  { id: 4, name: '桂溪生态公园', dist: '3.1km', note: '草坪大，适合大型犬', stars: 4.2, lat: 30.5636, lng: 104.0753,
    isTreasure: false, tags: ['适合大型犬'] },
  { id: 5, name: '沙河堡郊野公园', dist: '4.2km', note: '人少野趣足，有围栏草坪', stars: 4.6, lat: 30.6286, lng: 104.1053,
    isTreasure: true, treasureReason: '全围栏草坪，胆崽也能放心脱绳', hot: '本周 52 只崽崽来过', tags: ['有围栏', '人少清静', '可脱绳'] },
];

// ===== 候选玩伴池（碰个爪可发现 / 地图上开放定位的陌生汪） =====
const dogFriendsPool = [
  { id: 'f1', name: '奶茶妈妈', avatar: '🧋', isWalking: true, openToAll: false, loc: { lat: 30.6592, lng: 104.0560 },
    dogs: [{ name: '奶茶', emoji: '🐕', breed: '柯基', size: '小型', age: '青年', color: '棕色', weight: 10, tags: ['好动爱玩'], likes: ['零食', '奔跑'], vaccine: '已接种' }],
    postIds: ['p1', 'p2'] },
  { id: 'f2', name: '大壮爸比', avatar: '🧔', isWalking: true, openToAll: true, loc: { lat: 30.6442, lng: 104.0346 },
    dogs: [{ name: '大壮', emoji: '🐕‍🦺', breed: '拉布拉多', size: '大型', age: '青年', color: '黑色', weight: 30, tags: ['社会化良好', '活泼'], likes: ['游泳', '社交'], vaccine: '已接种' }],
    postIds: ['p3'] },
  { id: 'f3', name: '雪球姐姐', avatar: '👩', isWalking: false, openToAll: true, loc: { lat: 30.5840, lng: 104.0660 },
    dogs: [{ name: '雪球', emoji: '🐶', breed: '萨摩耶', size: '大型', age: '青年', color: '白色', weight: 22, tags: ['温顺', '粘人'], likes: ['奔跑', '社交'], vaccine: '已接种' }],
    postIds: ['p4'] },
  { id: 'f4', name: '布丁麻麻', avatar: '👩‍🦰', isWalking: false, openToAll: false, loc: { lat: 30.5640, lng: 104.0760 },
    dogs: [{ name: '布丁', emoji: '🦮', breed: '金毛', size: '大型', age: '老年', color: '黄色', weight: 26, tags: ['温顺'], likes: ['安静', '零食'], vaccine: '已接种' }],
    postIds: [] },
];

let myFriends = [];                    // 碰个爪添加的玩伴（池对象 + shareMyLocTo）
let myLocationPrivacy = { mode: 'friends' };  // 'friends' | 'everyone' | 'off'
let walkRecords = [];                  // 崽崽手账数据源

// 崽崽圈动态（仅 authorId ∈ myFriends 或 'me' 可见）
let circlePosts = [
  { id: 'p1', authorId: 'f1', authorName: '奶茶妈妈', authorAvatar: '🧋', dogName: '奶茶', dogEmoji: '🐕',
    cover: { gradient: 'linear-gradient(135deg, #F7C873 0%, #F4A698 100%)', emoji: '🐕' },
    text: '今天傍晚的人民公园，奶茶交到新朋友啦～', time: '昨天 18:20', likes: 6, likedByMe: false, type: 'normal' },
  { id: 'p2', authorId: 'f1', authorName: '奶茶妈妈', authorAvatar: '🧋', dogName: '奶茶', dogEmoji: '🐕',
    cover: { gradient: 'linear-gradient(135deg, #A8D4E4 0%, #6BA3BE 100%)', emoji: '🐾' },
    text: '小短腿也要努力跑步！', time: '3 天前', likes: 12, likedByMe: false, type: 'normal' },
  { id: 'p3', authorId: 'f2', authorName: '大壮爸比', authorAvatar: '🧔', dogName: '大壮', dogEmoji: '🐕‍🦺',
    cover: { gradient: 'linear-gradient(135deg, #6BA3BE 0%, #5A9BB5 100%)', emoji: '🏊' },
    text: '大壮第一次下水，居然直接学会了狗刨！', time: '2 天前', likes: 9, likedByMe: false, type: 'normal' },
  { id: 'p4', authorId: 'f3', authorName: '雪球姐姐', authorAvatar: '👩', dogName: '雪球', dogEmoji: '🐶',
    cover: { gradient: 'linear-gradient(135deg, #FFF0ED 0%, #F4A698 100%)', emoji: '❄️' },
    text: '雪球的微笑营业中～', time: '5 天前', likes: 15, likedByMe: false, type: 'normal' },
];

// 晒崽崽封面预设（渐变 + emoji）
const COVER_PRESETS = [
  { gradient: 'linear-gradient(135deg, #F7C873 0%, #F4A698 100%)', emoji: '🌞' },
  { gradient: 'linear-gradient(135deg, #A8D4E4 0%, #6BA3BE 100%)', emoji: '🌊' },
  { gradient: 'linear-gradient(135deg, #D9EAD3 0%, #95C795 100%)', emoji: '🌿' },
  { gradient: 'linear-gradient(135deg, #FFF0ED 0%, #F4A698 100%)', emoji: '🌸' },
  { gradient: 'linear-gradient(135deg, #E8F2F7 0%, #B8C9D9 100%)', emoji: '🌙' },
];
```

- [ ] **Step 2: 重写撒欢地渲染（`demo/app.js` 替换 initPlacesPage）**

```js
// ===== Places Page（撒欢地：搜索 + 宝藏标记） =====
function renderPlacesList(keyword) {
  const list = document.getElementById('placesList');
  const kw = (keyword || '').trim().toLowerCase();
  const filtered = kw
    ? spotsData.filter(p => (p.name + p.note + (p.tags || []).join('') + (p.treasureReason || '')).toLowerCase().includes(kw))
    : spotsData;

  if (filtered.length === 0) {
    list.innerHTML = '<div class="places-empty" id="placesEmpty">没找到相关撒欢地～换个词试试？</div>';
    return;
  }

  list.innerHTML = filtered.map((p, i) => `
    <div class="place-card animate-in ${p.isTreasure ? 'treasure' : ''}" style="animation-delay: ${i * 0.06}s" data-spot-id="${p.id}">
      <div class="place-card-inner">
        <div class="place-card-top">
          <span class="place-card-name">${p.isTreasure ? '💎 ' : ''}${p.name}</span>
          <span class="place-card-dist">${p.dist}</span>
        </div>
        <div class="place-card-note">${p.note}</div>
        ${p.isTreasure ? `
          <div class="place-card-treasure">
            <div class="treasure-badge">💎 宝藏撒欢地</div>
            <div class="treasure-reason">${p.treasureReason}</div>
            <div class="treasure-hot">🔥 ${p.hot}</div>
          </div>
          <div class="place-card-spottags">${(p.tags || []).map(t => `<span class="spot-feature-tag">${t}</span>`).join('')}</div>
        ` : ''}
        <span class="place-card-tag">
          <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"/></svg>
          推荐
          <span class="place-card-stars">${renderStars(p.stars)}</span>
        </span>
      </div>
    </div>
  `).join('');

  list.querySelectorAll('.place-card').forEach(card => {
    card.addEventListener('click', () => {
      const spot = spotsData.find(s => s.id === parseInt(card.dataset.spotId));
      if (!spot) return;
      switchTab('map');
      setTimeout(() => {
        map.flyTo([spot.lat, spot.lng], 16, { duration: 1.0 });
        showSpotCard({ name: spot.name, count: Math.floor(20 + Math.random() * 25) });
      }, 400);
    });
  });
}

function initPlacesPage() {
  renderPlacesList('');
  document.getElementById('placesSearch').addEventListener('input', (e) => {
    renderPlacesList(e.target.value);
  });
  document.getElementById('btnAddPlace').addEventListener('click', () => {
    alert('标记新地点功能开发中...');
  });
}
```

- [ ] **Step 3: 宝藏卡片样式（`demo/style.css` 追加）**

```css
/* ===== Task 2: 撒欢地搜索 + 宝藏 ===== */
.place-card.treasure {
  border: 1.5px solid rgba(247, 200, 115, 0.55);
  background: linear-gradient(180deg, #FFFDF7 0%, #FFFFFF 40%);
}
.place-card-spottags { display: flex; gap: 6px; flex-wrap: wrap; margin-top: 8px; }
.spot-feature-tag {
  font-size: var(--fs-micro); padding: 3px 8px; border-radius: var(--r-tag);
  background: var(--c-primary-light); color: var(--c-primary); font-weight: 600;
}
.place-card-treasure { margin-top: 8px; }
.treasure-badge {
  display: inline-block; font-size: var(--fs-micro); font-weight: 700;
  color: var(--c-gold-dark); background: #FDF6E3;
  padding: 3px 8px; border-radius: var(--r-tag); margin-bottom: 4px;
}
.treasure-reason { font-size: var(--fs-caption); color: var(--c-text-secondary); line-height: 1.5; }
.treasure-hot { font-size: var(--fs-micro); color: var(--c-peach); margin-top: 4px; font-weight: 600; }
.places-empty { text-align: center; color: var(--c-text-tertiary); font-size: var(--fs-body); padding: 40px 0; }
```

- [ ] **Step 4: 验证（绿）**

`?v=3`：撒欢地页 5 张卡片，2 张宝藏卡（人民公园/浣花溪）有金色描边+角标+理由+热度；搜索「围栏」只剩沙河堡；搜索「zzz」显示空状态；清空恢复；点任意卡片切到地图并 flyTo + 弹地点卡。控制台无错误。

- [ ] **Step 5: 提交**

```powershell
$env:GIT_AUTHOR_NAME='cpking0715'; $env:GIT_AUTHOR_EMAIL='cpking0715@users.noreply.github.com'; $env:GIT_COMMITTER_NAME='cpking0715'; $env:GIT_COMMITTER_EMAIL='cpking0715@users.noreply.github.com'; git add demo/; git commit -m "feat: 撒欢地搜索+宝藏标记，新增社交数据模型（玩伴池/崽崽圈/手账）"
```

---

### Task 3: 碰个爪（bumpOverlay 四阶段流程）

**Files:**
- Modify: `demo/app.js`（碰个爪全流程）
- Modify: `demo/style.css`（浮层与动画）

- [ ] **Step 1: 碰个爪逻辑（`demo/app.js`，追加到文件尾部）**

```js
// ===== 碰个爪（Bump） =====
let bumpTimer = null;
let bumpCandidate = null;

function isFriend(id) { return myFriends.some(f => f.id === id); }

function updateFriendCounts() {
  const n = myFriends.length;
  document.getElementById('friendsCount').textContent = '(' + n + ')';
  document.getElementById('menuFriendsBadge').textContent = n + '位';
}

function pickBumpCandidate(targetId) {
  if (targetId) {
    const t = dogFriendsPool.find(f => f.id === targetId && !isFriend(targetId));
    if (t) return t;
  }
  const candidates = dogFriendsPool.filter(f => !isFriend(f.id));
  if (!candidates.length) return null;
  const walking = candidates.filter(f => f.isWalking);
  const pool = walking.length ? walking : candidates;
  return pool[Math.floor(Math.random() * pool.length)];
}

function showBumpStage(stage) {
  ['Approach', 'Radar', 'Result', 'Success', 'Empty'].forEach(s => {
    document.getElementById('bumpStage' + s).style.display = (s === stage) ? '' : 'none';
  });
}

function openBumpOverlay(targetId) {
  document.getElementById('bumpOverlay').classList.add('show');
  showBumpStage('Approach');
  clearTimeout(bumpTimer);
  bumpTimer = setTimeout(() => showBumpStage('Radar'), 900);
  bumpTimer = setTimeout(() => {
    const c = pickBumpCandidate(targetId);
    if (!c) { showBumpStage('Empty'); return; }
    bumpCandidate = c;
    document.getElementById('bumpCandidateAvatar').textContent = c.avatar;
    document.getElementById('bumpCandidateName').textContent = c.name;
    document.getElementById('bumpCandidateDist').textContent = '距离你约 ' + (80 + Math.floor(Math.random() * 300)) + 'm';
    document.getElementById('bumpCandidateDogs').innerHTML = c.dogs.map(d =>
      `<span class="bump-dog-chip">${d.emoji} ${d.name} · ${d.breed}</span>`).join('');
    showBumpStage('Result');
  }, 2600);
}

function closeBumpOverlay() {
  clearTimeout(bumpTimer);
  document.getElementById('bumpOverlay').classList.remove('show');
}

function initBump() {
  document.getElementById('btnBump').addEventListener('click', () => openBumpOverlay());
  document.getElementById('btnBumpFromFriends').addEventListener('click', () => openBumpOverlay());
  document.getElementById('btnBumpClose').addEventListener('click', closeBumpOverlay);
  document.getElementById('btnBumpEmptyClose').addEventListener('click', closeBumpOverlay);

  document.getElementById('btnBumpConfirm').addEventListener('click', () => {
    if (!bumpCandidate || isFriend(bumpCandidate.id)) return;
    myFriends.push({ ...bumpCandidate, shareMyLocTo: true });
    updateFriendCounts();
    renderFriendMarkers();       // Task 6 实现；先存在即可
    // 成功撒花爪印
    document.getElementById('bumpSuccessPaws').innerHTML =
      Array.from({ length: 8 }, () => '<span class="bump-flying-paw">🐾</span>').join('');
    showBumpStage('Success');
  });

  document.getElementById('btnBumpViewDogs').addEventListener('click', () => {
    const f = bumpCandidate;
    closeBumpOverlay();
    if (f) openFriendDetail(f.id);
  });

  document.getElementById('btnBumpAgain').addEventListener('click', () => {
    bumpCandidate = null;
    openBumpOverlay();
  });
}
```

在 `DOMContentLoaded` 初始化列表末尾追加 `initBump();`。

- [ ] **Step 2: 浮层样式与动画（`demo/style.css` 追加）**

```css
/* ===== Task 3: 碰个爪浮层 ===== */
.bump-overlay {
  position: absolute; inset: 0; z-index: 60;
  background: radial-gradient(circle at 50% 40%, rgba(38, 50, 58, 0.92) 0%, rgba(24, 32, 38, 0.97) 100%);
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  opacity: 0; pointer-events: none; transition: opacity 0.3s var(--ease-decel, ease);
}
.bump-overlay.show { opacity: 1; pointer-events: auto; }
.bump-overlay .bump-stage { display: flex; flex-direction: column; align-items: center; gap: 18px; padding: 0 24px; width: 100%; box-sizing: border-box; }
.bump-close {
  position: absolute; top: 16px; right: 16px; z-index: 2;
  width: 32px; height: 32px; border-radius: 50%; border: none;
  background: rgba(255,255,255,0.15); color: #fff; font-size: 14px;
}
.bump-tip { color: rgba(255,255,255,0.85); font-size: var(--fs-body); text-align: center; }

/* 阶段1: 靠近 */
.bump-paw { font-size: 44px; }
.stage-approach { flex-direction: row !important; flex-wrap: wrap; justify-content: center; }
.stage-approach .bump-paw.paw-left { animation: pawLeft 0.9s ease forwards; }
.stage-approach .bump-paw.paw-right { animation: pawRight 0.9s ease forwards; transform: scaleX(-1); }
.stage-approach .bump-tip { width: 100%; }
@keyframes pawLeft { from { transform: translateX(-70px); opacity: 0.3; } to { transform: translateX(-6px); opacity: 1; } }
@keyframes pawRight { from { transform: translateX(70px) scaleX(-1); opacity: 0.3; } to { transform: translateX(6px) scaleX(-1); opacity: 1; } }

/* 阶段2: 雷达 */
.bump-radar { position: relative; width: 140px; height: 140px; display: flex; align-items: center; justify-content: center; }
.bump-radar-ring {
  position: absolute; inset: 0; border-radius: 50%;
  border: 2px solid rgba(247, 200, 115, 0.7);
  animation: radarPing 1.8s ease-out infinite;
}
.bump-radar-ring:nth-child(2) { animation-delay: 0.6s; }
.bump-radar-ring:nth-child(3) { animation-delay: 1.2s; }
.bump-radar-core { font-size: 40px; }
@keyframes radarPing { from { transform: scale(0.3); opacity: 0.9; } to { transform: scale(1.15); opacity: 0; } }

/* 阶段3: 发现 */
.bump-found-tip { color: var(--c-gold); font-weight: 700; font-size: 15px; }
.bump-candidate-card {
  width: 100%; background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.15);
  border-radius: var(--r-card); padding: 20px; text-align: center;
  animation: bumpCardIn 0.4s var(--ease-decel, ease);
}
@keyframes bumpCardIn { from { transform: translateY(20px) scale(0.95); opacity: 0; } to { transform: none; opacity: 1; } }
.bump-candidate-avatar { font-size: 48px; }
.bump-candidate-name { color: #fff; font-size: 17px; font-weight: 700; margin-top: 6px; }
.bump-candidate-dist { color: rgba(255,255,255,0.6); font-size: var(--fs-caption); margin-top: 2px; }
.bump-candidate-dogs { margin-top: 10px; display: flex; gap: 8px; justify-content: center; flex-wrap: wrap; }
.bump-dog-chip {
  background: rgba(247,200,115,0.15); color: var(--c-gold);
  border-radius: var(--r-pill); padding: 4px 12px; font-size: var(--fs-caption); font-weight: 600;
}
.bump-confirm-btn { width: 100%; }

/* 阶段4: 成功 */
.bump-success-icon { font-size: 56px; animation: bumpPop 0.5s var(--ease-decel, ease); }
@keyframes bumpPop { 0% { transform: scale(0); } 70% { transform: scale(1.25); } 100% { transform: scale(1); } }
.bump-success-paws { position: absolute; inset: 0; pointer-events: none; }
.bump-flying-paw {
  position: absolute; left: 50%; top: 45%; font-size: 20px;
  animation: pawFly 1s ease-out forwards;
}
.bump-flying-paw:nth-child(8n+1) { --dx: -90px; --dy: -110px; }
.bump-flying-paw:nth-child(8n+2) { --dx: 80px;  --dy: -120px; animation-delay: 0.05s; }
.bump-flying-paw:nth-child(8n+3) { --dx: -120px; --dy: -20px; animation-delay: 0.1s; }
.bump-flying-paw:nth-child(8n+4) { --dx: 120px; --dy: -40px;  animation-delay: 0.15s; }
.bump-flying-paw:nth-child(8n+5) { --dx: -60px; --dy: 90px;   animation-delay: 0.2s; }
.bump-flying-paw:nth-child(8n+6) { --dx: 70px;  --dy: 100px;  animation-delay: 0.25s; }
.bump-flying-paw:nth-child(8n+7) { --dx: 0;     --dy: -140px; animation-delay: 0.3s; }
.bump-flying-paw:nth-child(8n)   { --dx: 0;     --dy: 120px;  animation-delay: 0.35s; }
@keyframes pawFly { from { transform: translate(0,0) scale(0.6); opacity: 1; } to { transform: translate(var(--dx), var(--dy)) scale(1.1); opacity: 0; } }
.bump-success-tip { color: #fff; font-size: 16px; font-weight: 700; }
.bump-success-actions { display: flex; gap: 12px; width: 100%; }
.bump-success-actions button { flex: 1; }
.bump-success-actions .btn-reset { background: rgba(255,255,255,0.12); color: #fff; }

/* 阶段5: 空 */
.bump-empty-icon { font-size: 48px; opacity: 0.8; }
.bump-overlay .btn-reset { color: rgba(255,255,255,0.85); }
```

- [ ] **Step 3: 验证（绿）**

`?v=4`：地图页点 🐾 → 爪印靠近动画 → 雷达波纹「正在感受附近的崽崽…」→ 候选卡片（奶茶妈妈或大壮爸比，因为 isWalking）→ 点「碰个爪，交玩伴」→ 爪印撒花 → 「看看 ta 家崽崽」（Task 4 实现档案页，未实现前若报错先验证 Success 阶段）与「继续碰碰」（第二次碰出另一位；连续碰完 4 位后出现空状态文案）。✕ 可随时关闭。

- [ ] **Step 4: 提交**

```powershell
$env:GIT_AUTHOR_NAME='cpking0715'; $env:GIT_AUTHOR_EMAIL='cpking0715@users.noreply.github.com'; $env:GIT_COMMITTER_NAME='cpking0715'; $env:GIT_COMMITTER_EMAIL='cpking0715@users.noreply.github.com'; git add demo/; git commit -m "feat: 碰个爪交玩伴（四阶段动画 + 候选池 + 空状态）"
```

---

### Task 4: 玩伴列表 + ta 家崽崽档案

**Files:**
- Modify: `demo/app.js`（renderFriendsList、开关/删除、openFriendDetail）
- Modify: `demo/style.css`（玩伴卡片/开关/档案样式）

- [ ] **Step 1: 玩伴列表逻辑（`demo/app.js` 追加）**

```js
// ===== 玩伴管理 =====
let confirmAction = null;

function openConfirmModal(text, action) {
  document.getElementById('confirmText').textContent = text;
  confirmAction = action;
  openModal('confirmModal');
}

function initConfirmModal() {
  document.getElementById('btnConfirmCancel').addEventListener('click', () => closeModal('confirmModal'));
  document.getElementById('btnConfirmOk').addEventListener('click', () => {
    closeModal('confirmModal');
    if (confirmAction) { confirmAction(); confirmAction = null; }
  });
}

function renderFriendsList() {
  const list = document.getElementById('friendsList');
  if (!myFriends.length) {
    list.innerHTML = `
      <div class="friends-empty">
        <div class="friends-empty-icon">🐾</div>
        <div class="friends-empty-text">还没有玩伴，去碰个爪认识新朋友吧～</div>
        <button class="btn-confirm" id="btnBumpFromEmpty">碰个爪 🐾</button>
      </div>`;
    document.getElementById('btnBumpFromEmpty').addEventListener('click', () => openBumpOverlay());
    return;
  }

  list.innerHTML = myFriends.map(f => `
    <div class="friend-card" data-friend-id="${f.id}">
      <div class="friend-card-main" data-action="detail">
        <div class="friend-avatar">${f.avatar}</div>
        <div class="friend-info">
          <div class="friend-name">${f.name}
            <span class="friend-walk-status ${f.isWalking ? 'walking' : ''}">
              <span class="friend-status-dot"></span>${f.isWalking ? '正在遛弯' : '在家休息'}
            </span>
          </div>
          <div class="friend-dogs">${f.dogs.map(d => `<span class="friend-dog-chip">${d.emoji} ${d.name}</span>`).join('')}</div>
        </div>
      </div>
      <div class="friend-card-actions">
        <label class="friend-loc-switch">
          <span class="friend-loc-label">让 ta 看见我家崽崽</span>
          <input type="checkbox" data-action="loc" ${f.shareMyLocTo ? 'checked' : ''} />
          <span class="friend-loc-slider"></span>
        </label>
        <button class="friend-remove" data-action="remove">删除玩伴</button>
      </div>
    </div>
  `).join('');

  list.querySelectorAll('.friend-card').forEach(card => {
    const f = myFriends.find(x => x.id === card.dataset.friendId);
    card.querySelector('[data-action="detail"]').addEventListener('click', () => openFriendDetail(f.id));
    card.querySelector('input[data-action="loc"]').addEventListener('change', (e) => {
      f.shareMyLocTo = e.target.checked;
    });
    card.querySelector('[data-action="remove"]').addEventListener('click', (e) => {
      e.stopPropagation();
      openConfirmModal('和 ' + f.name + ' 解除玩伴关系？ta 的崽崽动态将从崽崽圈消失～', () => {
        myFriends = myFriends.filter(x => x.id !== f.id);
        updateFriendCounts();
        renderFriendsList();
        renderFriendMarkers();
        if (currentTab === 'circle') renderCircleTimeline();
      });
    });
  });
}

function openFriendDetail(friendId) {
  const f = dogFriendsPool.find(x => x.id === friendId) || myFriends.find(x => x.id === friendId);
  if (!f) return;
  document.getElementById('friendDetailTitle').textContent = 'ta 家崽崽';
  document.getElementById('friendDetailOwner').innerHTML = `
    <div class="friend-avatar big">${f.avatar}</div>
    <div class="friend-detail-owner-name">${f.name} 的崽崽们</div>`;
  document.getElementById('friendDogsList').innerHTML = f.dogs.map(d => `
    <div class="friend-dog-profile">
      <div class="dog-info-head">
        <div class="dog-info-avatar">${d.emoji}</div>
        <div class="dog-info-head-right">
          <div class="dog-info-name">${d.name}</div>
          <div class="dog-info-breed">${d.breed} · ${d.size}犬 · ${d.age} · ${d.weight}kg</div>
        </div>
      </div>
      <div class="dog-info-row">
        <span class="dog-info-label">毛色</span>
        <span class="dog-info-color"><span class="dog-info-color-dot" style="background:${COLOR_HEX[d.color] || '#F7C873'}"></span>${d.color}</span>
      </div>
      <div class="dog-info-row">
        <span class="dog-info-label">脾气</span>
        <span class="dog-info-tags">${d.tags.map(t => '<span class="dog-tag trait">' + t + '</span>').join('')}</span>
      </div>
      <div class="dog-info-row">
        <span class="dog-info-label">喜好</span>
        <span class="dog-info-tags">${d.likes.map(t => '<span class="dog-tag trait">' + t + '</span>').join('')}</span>
      </div>
      <div class="dog-info-row">
        <span class="dog-info-label">疫苗</span>
        <span class="dog-info-vaccine">${d.vaccine}</span>
      </div>
    </div>
  `).join('');
  openSubPage('pageFriendDetail');
}

function initFriendsPages() {
  document.getElementById('btnBackFromFriends').addEventListener('click', () => closeSubPage('pageFriends'));
  document.getElementById('btnBackFromFriendDetail').addEventListener('click', () => {
    // 档案页总是从玩伴列表进入
    document.getElementById('pageFriendDetail').classList.remove('active');
    document.getElementById('pageFriends').classList.add('active');
  });
  initConfirmModal();
}
```

`DOMContentLoaded` 追加 `initFriendsPages();`。

注意：碰个爪成功后若当前在玩伴列表页需刷新——在 Task 3 的 `btnBumpConfirm` 处理里，成功动画前追加：`if (document.getElementById('pageFriends').classList.contains('active')) renderFriendsList();`

- [ ] **Step 2: 样式（`demo/style.css` 追加）**

```css
/* ===== Task 4: 玩伴列表/档案 ===== */
.friends-list { padding: var(--sp-md) var(--sp-lg) 30px; display: flex; flex-direction: column; gap: var(--sp-md); }
.friends-empty { text-align: center; padding: 60px 24px; }
.friends-empty-icon { font-size: 44px; }
.friends-empty-text { color: var(--c-text-secondary); font-size: var(--fs-body); margin: 12px 0 20px; }
.friend-card {
  background: var(--c-surface); border-radius: var(--r-card);
  box-shadow: var(--shadow-sm); padding: 14px 16px;
}
.friend-card-main { display: flex; gap: 12px; align-items: center; }
.friend-avatar {
  width: 44px; height: 44px; border-radius: 50%; flex-shrink: 0;
  background: var(--c-primary-light); display: flex; align-items: center; justify-content: center; font-size: 22px;
}
.friend-avatar.big { width: 56px; height: 56px; font-size: 28px; }
.friend-info { flex: 1; min-width: 0; }
.friend-name { font-size: var(--fs-card-title); font-weight: 600; color: var(--c-text); display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
.friend-walk-status {
  font-size: var(--fs-micro); font-weight: 600; color: var(--c-text-tertiary);
  display: inline-flex; align-items: center; gap: 4px;
}
.friend-walk-status.walking { color: #5FA97F; }
.friend-status-dot { width: 7px; height: 7px; border-radius: 50%; background: currentColor; }
.friend-walk-status.walking .friend-status-dot { animation: statusPulse 1.6s ease infinite; }
@keyframes statusPulse { 0%,100% { box-shadow: 0 0 0 0 rgba(95,169,127,0.5); } 50% { box-shadow: 0 0 0 5px rgba(95,169,127,0); } }
.friend-dogs { margin-top: 4px; display: flex; gap: 6px; flex-wrap: wrap; }
.friend-dog-chip {
  font-size: var(--fs-micro); background: var(--c-peach-light); color: var(--c-peach);
  padding: 2px 8px; border-radius: var(--r-pill); font-weight: 600;
}
.friend-card-actions {
  margin-top: 12px; padding-top: 10px; border-top: 1px dashed var(--c-border);
  display: flex; align-items: center; justify-content: space-between; gap: 8px;
}
/* 开关（让 ta 看见我家崽崽） */
.friend-loc-switch { position: relative; display: flex; align-items: center; gap: 8px; cursor: pointer; }
.friend-loc-label { font-size: var(--fs-caption); color: var(--c-text-secondary); }
.friend-loc-switch input { position: absolute; opacity: 0; width: 0; height: 0; }
.friend-loc-slider {
  width: 36px; height: 21px; border-radius: var(--r-pill); background: var(--c-border);
  position: relative; transition: background 0.2s; flex-shrink: 0;
}
.friend-loc-slider::after {
  content: ''; position: absolute; top: 2.5px; left: 3px;
  width: 16px; height: 16px; border-radius: 50%; background: #fff;
  box-shadow: var(--shadow-sm); transition: transform 0.2s;
}
.friend-loc-switch input:checked + .friend-loc-slider { background: var(--c-primary); }
.friend-loc-switch input:checked + .friend-loc-slider::after { transform: translateX(14px); }
.friend-remove {
  border: none; background: none; color: var(--c-text-tertiary);
  font-size: var(--fs-caption); padding: 4px 6px;
}
.friend-remove:active { color: var(--c-peach); }
/* ta 家崽崽 */
.friend-detail-owner { display: flex; align-items: center; gap: 12px; padding: var(--sp-md) var(--sp-lg) 0; }
.friend-detail-owner-name { font-size: var(--fs-card-title); font-weight: 600; color: var(--c-text); }
.friend-dogs-list { padding: var(--sp-md) var(--sp-lg) 30px; display: flex; flex-direction: column; gap: var(--sp-md); }
.friend-dog-profile {
  background: var(--c-surface); border-radius: var(--r-card);
  box-shadow: var(--shadow-sm); padding: 16px;
}
```

- [ ] **Step 3: 验证（绿）**

`?v=5`：碰个爪加一位玩伴 → 崽崽家菜单「玩伴(1位)」→ 进入列表：卡片含头像/名字/「正在遛弯」绿点脉冲或「在家休息」灰点/狗chip；开关默认开，可切换；点卡片主体进档案页（品种/毛色/脾气/喜好/疫苗完整，无定位信息），返回回到列表；「删除玩伴」弹确认，确认后列表消失、badge 归 0、崽崽圈（若已实现）动态消失。无玩伴时空状态+碰个爪按钮可用。

- [ ] **Step 4: 提交**

```powershell
$env:GIT_AUTHOR_NAME='cpking0715'; $env:GIT_AUTHOR_EMAIL='cpking0715@users.noreply.github.com'; $env:GIT_COMMITTER_NAME='cpking0715'; $env:GIT_COMMITTER_EMAIL='cpking0715@users.noreply.github.com'; git add demo/; git commit -m "feat: 玩伴列表（定位开关/遛弯状态/删除）+ ta 家崽崽档案页"
```

---

### Task 5: 小窝设置 ·「崽崽在哪」三级权限

**Files:**
- Modify: `demo/app.js`（renderSettingsPrivacy、三级切换、单独管理入口）
- Modify: `demo/style.css`（设置页样式）

- [ ] **Step 1: 设置页逻辑（`demo/app.js` 追加）**

```js
// ===== 小窝设置：崽崽在哪 =====
function renderSettingsPrivacy() {
  document.querySelectorAll('#pageSettings .privacy-option').forEach(opt => {
    opt.classList.toggle('active', opt.dataset.mode === myLocationPrivacy.mode);
  });
  document.getElementById('privacyOffNote').style.display =
    myLocationPrivacy.mode === 'off' ? '' : 'none';
}

function initSettingsPage() {
  document.querySelectorAll('#pageSettings .privacy-option').forEach(opt => {
    opt.addEventListener('click', () => {
      myLocationPrivacy.mode = opt.dataset.mode;
      renderSettingsPrivacy();
      renderFriendsList(); // 玩伴卡片状态文案可同步（若后续扩展）
    });
  });
  document.getElementById('btnGoPerFriendPrivacy').addEventListener('click', () => {
    document.getElementById('pageSettings').classList.remove('active');
    renderFriendsList();
    document.getElementById('pageFriends').classList.add('active');
  });
  document.getElementById('btnBackFromSettings').addEventListener('click', () => closeSubPage('pageSettings'));
}
```

`DOMContentLoaded` 追加 `initSettingsPage();`。

- [ ] **Step 2: 样式（`demo/style.css` 追加）**

```css
/* ===== Task 5: 小窝设置 ===== */
.settings-body { padding: var(--sp-md) var(--sp-lg) 30px; }
.settings-section-title { font-size: var(--fs-card-title); font-weight: 700; color: var(--c-text); margin-bottom: var(--sp-md); }
.settings-sub { font-size: var(--fs-caption); color: var(--c-text-tertiary); font-weight: 400; margin-left: 6px; }
.privacy-option {
  display: flex; gap: 12px; align-items: flex-start;
  background: var(--c-surface); border: 1.5px solid transparent;
  border-radius: var(--r-card); padding: 14px 16px; margin-bottom: var(--sp-sm);
  box-shadow: var(--shadow-sm); cursor: pointer;
}
.privacy-option.active { border-color: var(--c-primary); background: var(--c-primary-light); }
.privacy-radio {
  width: 18px; height: 18px; border-radius: 50%; flex-shrink: 0; margin-top: 2px;
  border: 2px solid var(--c-border); position: relative;
}
.privacy-option.active .privacy-radio { border-color: var(--c-primary); }
.privacy-option.active .privacy-radio::after {
  content: ''; position: absolute; inset: 3px; border-radius: 50%; background: var(--c-primary);
}
.privacy-name { font-size: var(--fs-body); font-weight: 600; color: var(--c-text); }
.privacy-desc { font-size: var(--fs-caption); color: var(--c-text-secondary); margin-top: 2px; line-height: 1.5; }
.settings-note {
  background: var(--c-peach-light); color: var(--c-peach); font-size: var(--fs-caption);
  border-radius: var(--r-tag); padding: 8px 12px; margin-top: 4px;
}
.settings-divider { height: 1px; background: var(--c-border); margin: var(--sp-lg) 0; }
.settings-link { color: var(--c-primary); font-size: var(--fs-body); font-weight: 600; }
```

- [ ] **Step 3: 验证（绿）**

`?v=6`：崽崽家 → 小窝设置：默认选中「仅玩伴可见」；点击「所有汪星可见」「关闭行踪」切换选中态，「关闭行踪」时出现提示条；「单独管理」跳到玩伴列表；返回正常。

- [ ] **Step 4: 提交**

```powershell
$env:GIT_AUTHOR_NAME='cpking0715'; $env:GIT_AUTHOR_EMAIL='cpking0715@users.noreply.github.com'; $env:GIT_COMMITTER_NAME='cpking0715'; $env:GIT_COMMITTER_EMAIL='cpking0715@users.noreply.github.com'; git add demo/; git commit -m "feat: 小窝设置「崽崽在哪」三级定位权限 + 单独管理入口"
```

---

### Task 6: 地图玩伴标记 + 陌生汪标记

**Files:**
- Modify: `demo/app.js`（renderFriendMarkers、好友卡/简卡、地图初始化接入）
- Modify: `demo/style.css`（弹跳/光环/普通标记样式）

- [ ] **Step 1: 标记渲染与卡片逻辑（`demo/app.js` 追加）**

```js
// ===== 地图：玩伴崽崽标记 + 陌生汪标记 =====
let friendMarkers = [];
let friendMoveTimer = null;

function hideFriendDogCard() {
  document.getElementById('friendDogCard').classList.remove('show');
}

function renderFriendMarkers() {
  if (!map) return;
  friendMarkers.forEach(m => map.removeLayer(m.marker));
  friendMarkers = [];

  // 1) 玩伴的崽崽：仅 isWalking 显示，弹跳 + 光环
  myFriends.filter(f => f.isWalking).forEach(f => {
    f.dogs.forEach((dog, di) => {
      const lat = f.loc.lat + di * 0.0009;
      const lng = f.loc.lng + di * 0.0007;
      const icon = L.divIcon({
        className: '',
        html: `<div class="friend-dog-marker"><div class="friend-marker-halo"></div><div class="friend-marker-icon">${dog.emoji}</div><div class="friend-marker-name">${dog.name}</div></div>`,
        iconSize: [56, 64], iconAnchor: [28, 36],
      });
      const marker = L.marker([lat, lng], { icon, zIndexOffset: 1200 }).addTo(map);
      marker.on('click', (e) => {
        L.DomEvent.stopPropagation(e);
        showFriendDogCard(f, dog, true);
      });
      friendMarkers.push({ marker, f, lat, lng });
    });
  });

  // 2) 开放定位的陌生汪：openToAll && isWalking && 非玩伴，普通样式
  dogFriendsPool.filter(f => f.openToAll && f.isWalking && !isFriend(f.id)).forEach(f => {
    f.dogs.forEach((dog, di) => {
      const lat = f.loc.lat + di * 0.0009;
      const lng = f.loc.lng + di * 0.0007;
      const icon = L.divIcon({
        className: '',
        html: `<div class="stranger-dog-marker"><div class="stranger-marker-icon">${dog.emoji}</div><div class="friend-marker-name">${dog.name}</div></div>`,
        iconSize: [50, 56], iconAnchor: [25, 30],
      });
      const marker = L.marker([lat, lng], { icon, zIndexOffset: 1100 }).addTo(map);
      marker.on('click', (e) => {
        L.DomEvent.stopPropagation(e);
        showFriendDogCard(f, dog, false);
      });
      friendMarkers.push({ marker, f, lat, lng });
    });
  });

  // 缓慢随机移动（模拟遛弯，每 4 秒）
  clearInterval(friendMoveTimer);
  if (friendMarkers.length) {
    friendMoveTimer = setInterval(() => {
      friendMarkers.forEach(fm => {
        fm.lat += (Math.random() - 0.5) * 0.0002;
        fm.lng += (Math.random() - 0.5) * 0.0002;
        fm.marker.setLatLng([fm.lat, fm.lng]);
      });
    }, 4000);
  }
}

function showFriendDogCard(f, dog, isFriendOwner) {
  hideSpotCard(); hideDogCard();
  document.getElementById('friendDogAvatar').textContent = dog.emoji;
  document.getElementById('friendDogName').textContent = dog.name;
  document.getElementById('friendDogBreed').textContent =
    dog.breed + ' · ' + dog.size + '犬 · ' + dog.age + ' · ' + dog.weight + 'kg';

  const tagsRow = document.getElementById('friendDogTagsRow');
  if (dog.tags && dog.tags.length) {
    document.getElementById('friendDogTags').innerHTML = dog.tags.map(t => `<span class="dog-tag trait">${t}</span>`).join('');
    tagsRow.style.display = '';
  } else tagsRow.style.display = 'none';

  const likesRow = document.getElementById('friendDogLikesRow');
  if (dog.likes && dog.likes.length) {
    document.getElementById('friendDogLikes').innerHTML = dog.likes.map(t => `<span class="dog-tag trait">${t}</span>`).join('');
    likesRow.style.display = '';
  } else likesRow.style.display = 'none';

  const badge = document.getElementById('friendDogBadge');
  const bumpBtn = document.getElementById('btnFriendDogBump');
  badge.style.display = isFriendOwner ? '' : 'none';
  bumpBtn.style.display = isFriendOwner ? 'none' : '';
  bumpBtn.onclick = () => { hideFriendDogCard(); openBumpOverlay(f.id); };

  document.getElementById('friendDogCard').classList.add('show');
}
```

接入点 3 处：
1. `initMap()` 末尾（`btnLocate` 绑定之后）追加：`renderFriendMarkers();`
2. `map.on('click', ...)` 改为：`map.on('click', function () { hideSpotCard(); hideDogCard(); hideFriendDogCard(); });`
3. 碰个爪成功（Task 3 的 `btnBumpConfirm` 处理器）与删除玩伴处已调用 `renderFriendMarkers()`，无需再改

- [ ] **Step 2: 标记样式（`demo/style.css` 追加）**

```css
/* ===== Task 6: 地图玩伴/陌生汪标记 ===== */
.friend-dog-marker { position: relative; width: 56px; height: 64px; text-align: center; }
.friend-marker-halo {
  position: absolute; left: 50%; top: 20px; width: 46px; height: 46px;
  transform: translate(-50%, -50%); border-radius: 50%;
  border: 2.5px solid rgba(107, 163, 190, 0.85);
  animation: haloPulse 1.8s ease-out infinite;
}
@keyframes haloPulse {
  0% { box-shadow: 0 0 0 0 rgba(107,163,190,0.45); }
  100% { box-shadow: 0 0 0 14px rgba(107,163,190,0); }
}
.friend-marker-icon {
  position: absolute; left: 50%; top: 20px; transform: translate(-50%, -50%);
  width: 36px; height: 36px; border-radius: 50%;
  background: #fff; border: 2.5px solid var(--c-primary);
  display: flex; align-items: center; justify-content: center; font-size: 19px;
  box-shadow: var(--shadow-md);
  animation: friendMarkerBounce 1.3s ease-in-out infinite;
}
@keyframes friendMarkerBounce {
  0%, 100% { transform: translate(-50%, -50%); }
  50% { transform: translate(-50%, calc(-50% - 7px)); }
}
.friend-marker-name {
  position: absolute; left: 50%; bottom: 0; transform: translateX(-50%);
  background: rgba(61,61,61,0.85); color: #fff; white-space: nowrap;
  font-size: var(--fs-micro); padding: 2px 7px; border-radius: var(--r-pill);
}
.stranger-dog-marker { position: relative; width: 50px; height: 56px; text-align: center; }
.stranger-marker-icon {
  position: absolute; left: 50%; top: 18px; transform: translate(-50%, -50%);
  width: 32px; height: 32px; border-radius: 50%;
  background: #fff; border: 2px solid var(--c-border);
  display: flex; align-items: center; justify-content: center; font-size: 17px;
  box-shadow: var(--shadow-sm);
}
.friend-badge {
  display: inline-block; font-size: var(--fs-micro); font-weight: 700;
  background: var(--c-primary-light); color: var(--c-primary);
  padding: 2px 8px; border-radius: var(--r-pill); vertical-align: 2px; margin-left: 6px;
}
```

- [ ] **Step 3: 验证（绿）**

`?v=7`：初始地图：浣花溪附近有陌生汪「大壮」普通标记（无光环无弹跳）→ 点击弹简卡（含「碰碰爪交个玩伴」按钮，点击直接定位到该候选进入碰个爪流程）；碰个爪加奶茶妈妈（isWalking）→ 人民公园附近出现「奶茶」弹跳+光环标记，每 4 秒缓慢移动，点击弹档案卡带「玩伴」徽章；删除玩伴后标记消失；点击地图空白处卡片收起。

- [ ] **Step 4: 提交**

```powershell
$env:GIT_AUTHOR_NAME='cpking0715'; $env:GIT_AUTHOR_EMAIL='cpking0715@users.noreply.github.com'; $env:GIT_COMMITTER_NAME='cpking0715'; $env:GIT_COMMITTER_EMAIL='cpking0715@users.noreply.github.com'; git add demo/; git commit -m "feat: 地图玩伴崽崽弹跳光环标记 + 陌生汪标记与碰爪引导"
```

---

### Task 7: 崽崽战报 + 崽崽手账（轨迹采样/结束流程改造）

**Files:**
- Modify: `demo/app.js`（轨迹采样、距离计算、confirmEndWalk 改造、战报页/手账页/晒圈弹窗）
- Modify: `demo/style.css`（战报/手账样式）

- [ ] **Step 1: 轨迹采样与距离（`demo/app.js`）**

1. `walkState` 对象追加字段：`track: []`（L41-47）
2. 顶部工具函数区追加：

```js
// 经纬度距离（米），简化球面公式
function distMeters(lat1, lng1, lat2, lng2) {
  const R = 6371000, rad = Math.PI / 180;
  const dLat = (lat2 - lat1) * rad, dLng = (lng2 - lng1) * rad;
  const a = Math.sin(dLat / 2) ** 2 + Math.cos(lat1 * rad) * Math.cos(lat2 * rad) * Math.sin(dLng / 2) ** 2;
  return 2 * R * Math.asin(Math.sqrt(a));
}

function trackDistance(track) {
  let d = 0;
  for (let i = 1; i < track.length; i++) {
    d += distMeters(track[i - 1].lat, track[i - 1].lng, track[i].lat, track[i].lng);
  }
  return d;
}

function formatDist(meters) {
  return meters >= 1000 ? (meters / 1000).toFixed(1) + 'km' : Math.round(meters) + 'm';
}

function formatDuration(totalSeconds) {
  const m = Math.floor(totalSeconds / 60), s = totalSeconds % 60;
  return m.toString().padStart(2, '0') + ':' + s.toString().padStart(2, '0');
}
```

3. `startWalkingPage()` 内 `walkState.startTime = new Date();` 之后追加：

```js
  walkState.track = [{ lat: walkState.startLat, lng: walkState.startLng }];
```

4. `showWalkMarkersOnMap()` 的 `walkMoveTimer` 回调内，`dogMarkers[i].setLatLng(...)` 之后追加轨迹采样：

```js
        // 轨迹采样（以第一只狗位置为准，切 Tab 不中断）
        if (i === 0 && walkState.isWalking) {
          walkState.track.push({ lat: dog._lat, lng: dog._lng });
        }
```

5. `updateWalkStatusBar(false)` 调用处距离改为基于轨迹（同一 `walkMapTimer` 回调内）：把 `walkDistance += Math.floor(1.2 + Math.random() * 0.8);` 替换为：

```js
      walkDistance = trackDistance(walkState.track);
```

（`formatDist` 与状态栏显示逻辑兼容米数，无需改 `updateWalkStatusBar`。）

- [ ] **Step 2: 结束流程改造（`confirmEndWalk` 重写）**

```js
let currentSummaryRecord = null;
let summaryMap = null;
let summaryLayers = [];

function confirmEndWalk() {
  closeModal('endWalkModal');

  const duration = walkSeconds;
  const distance = Math.max(trackDistance(walkState.track), Math.round(walkDistance));
  const totalWeight = walkState.dogs.reduce((s, d) => {
    const src = dogsData.find(x => x.id === d.id);
    return s + ((src && src.weight) || 10);
  }, 0);
  const calories = Math.max(1, Math.round(totalWeight * (distance / 1000) * 1.2));

  const record = {
    id: Date.now(),
    date: new Date().toLocaleString('zh-CN', { month: 'numeric', day: 'numeric', hour: '2-digit', minute: '2-digit' }),
    duration, distance, calories,
    track: walkState.track.slice(),
    dogs: walkState.dogs.map(d => ({ name: d.name, emoji: d.emoji })),
    published: false,
  };
  walkRecords.unshift(record);

  // 清除遛狗状态（保留原逻辑）
  hideWalkMarkers();
  walkState.isWalking = false;
  walkState.dogs = [];
  walkState.startTime = null;
  walkState.track = [];
  walkSeconds = 0;
  walkDistance = 0;

  isWalkingActive = false;
  clearInterval(walkingTimer);
  walkingSeconds = 0;
  document.getElementById('timerProgressPage').style.strokeDashoffset = 0;
  document.getElementById('walkingPageActive').style.display = 'none';
  document.getElementById('btnStartWalkPage').style.display = '';
  document.querySelector('#pageWalking .walking-location').style.display = '';
  document.querySelector('#pageWalking .dog-select-list').style.display = '';
  document.querySelector('#pageWalking .add-dog-inline').style.display = '';
  renderDogPageSelectList();

  openWalkSummary(record);
}
```

- [ ] **Step 3: 战报页与手账页（`demo/app.js` 追加）**

```js
// ===== 崽崽战报 =====
function openWalkSummary(record) {
  currentSummaryRecord = record;
  document.getElementById('summaryDuration').textContent = formatDuration(record.duration);
  document.getElementById('summaryDistance').textContent = formatDist(record.distance);
  document.getElementById('summaryCalories').textContent = record.calories;
  document.getElementById('summaryDogs').innerHTML =
    record.dogs.map(d => `<span class="summary-dog">${d.emoji} ${d.name}</span>`).join('');
  const pubBtn = document.getElementById('btnPublishWalk');
  pubBtn.textContent = record.published ? '已晒到崽崽圈 ✓' : '晒到崽崽圈';
  pubBtn.disabled = record.published;
  pubBtn.classList.toggle('disabled', record.published);
  openSubPage('pageWalkSummary');
  setTimeout(() => renderSummaryTrack(record), 120);
}

function renderSummaryTrack(record) {
  const el = document.getElementById('summaryMap');
  if (!summaryMap) {
    summaryMap = L.map(el, { zoomControl: false, attributionControl: false, dragging: false, touchZoom: false, scrollWheelZoom: false });
    L.tileLayer('https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png', { maxZoom: 19, subdomains: 'abcd' }).addTo(summaryMap);
  }
  summaryMap.invalidateSize();
  summaryLayers.forEach(l => summaryMap.removeLayer(l));
  summaryLayers = [];
  if (!record.track || record.track.length < 2) return;
  const latlngs = record.track.map(p => [p.lat, p.lng]);
  const line = L.polyline(latlngs, { color: '#6BA3BE', weight: 4, opacity: 0.9 }).addTo(summaryMap);
  const start = L.circleMarker(latlngs[0], { radius: 6, fillColor: '#5FA97F', fillOpacity: 1, color: '#fff', weight: 2 }).addTo(summaryMap);
  const end = L.circleMarker(latlngs[latlngs.length - 1], { radius: 6, fillColor: '#F4A698', fillOpacity: 1, color: '#fff', weight: 2 }).addTo(summaryMap);
  summaryLayers.push(line, start, end);
  summaryMap.fitBounds(line.getBounds(), { padding: [24, 24] });
}

function initWalkSummaryPage() {
  document.getElementById('btnSummaryDone').addEventListener('click', () => {
    closeSubPage('pageWalkSummary');
  });
  document.getElementById('btnPublishWalk').addEventListener('click', () => {
    if (!currentSummaryRecord || currentSummaryRecord.published) return;
    openModal('publishWalkModal');
    document.getElementById('publishWalkStats').innerHTML =
      `<span>⏱ ${formatDuration(currentSummaryRecord.duration)}</span><span>📍 ${formatDist(currentSummaryRecord.distance)}</span><span>🔥 ${currentSummaryRecord.calories} 大卡</span>`;
    document.getElementById('publishWalkText').value = '';
  });
  document.getElementById('btnPublishWalkConfirm').addEventListener('click', () => {
    const r = currentSummaryRecord;
    if (!r) return;
    circlePosts.unshift({
      id: 'post' + Date.now(), authorId: 'me', authorName: '我', authorAvatar: '🏠',
      dogName: r.dogs.map(d => d.name).join('、'), dogEmoji: r.dogs[0] ? r.dogs[0].emoji : '🐶',
      cover: { gradient: 'linear-gradient(135deg, #6BA3BE 0%, #5A9BB5 100%)', emoji: '🏅' },
      text: document.getElementById('publishWalkText').value.trim() || '今天的遛弯战报出炉啦～',
      time: '刚刚', likes: 0, likedByMe: false,
      type: 'walkReport',
      stats: { duration: r.duration, distance: r.distance, calories: r.calories },
    });
    r.published = true;
    closeModal('publishWalkModal');
    const pubBtn = document.getElementById('btnPublishWalk');
    pubBtn.textContent = '已晒到崽崽圈 ✓';
    pubBtn.disabled = true;
    pubBtn.classList.add('disabled');
  });
}

// ===== 崽崽手账 =====
function renderWalkRecords() {
  const list = document.getElementById('recordsList');
  if (!walkRecords.length) {
    list.innerHTML = '<div class="places-empty">还没有遛弯记录，带崽崽出去遛弯吧～</div>';
    return;
  }
  list.innerHTML = walkRecords.map(r => `
    <div class="record-card" data-record-id="${r.id}">
      <div class="record-card-top">
        <span class="record-date">${r.date}</span>
        ${r.published ? '<span class="record-published">已晒圈</span>' : ''}
      </div>
      <div class="record-stats">
        <span>⏱ ${formatDuration(r.duration)}</span>
        <span>📍 ${formatDist(r.distance)}</span>
        <span>🔥 ${r.calories} 大卡</span>
      </div>
      <div class="record-dogs">${r.dogs.map(d => `<span class="friend-dog-chip">${d.emoji} ${d.name}</span>`).join('')}</div>
    </div>
  `).join('');
  list.querySelectorAll('.record-card').forEach(card => {
    card.addEventListener('click', () => {
      const r = walkRecords.find(x => x.id === parseInt(card.dataset.recordId));
      if (r) openWalkSummary(r);
    });
  });
}

function initWalkRecordsPage() {
  document.getElementById('btnBackFromRecords').addEventListener('click', () => closeSubPage('pageWalkRecords'));
}
```

`DOMContentLoaded` 追加 `initWalkSummaryPage(); initWalkRecordsPage();`。

- [ ] **Step 4: 晒战报弹窗 HTML（`demo/index.html`，confirmModal 之后）**

```html
      <!-- 战报晒圈弹窗 -->
      <div class="modal-overlay" id="publishWalkModal">
        <div class="modal-sheet">
          <div class="modal-handle"></div>
          <div class="modal-header">
            <h2>晒到崽崽圈</h2>
            <button class="modal-close" data-close="publishWalkModal">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
            </button>
          </div>
          <div class="modal-body">
            <div class="publish-walk-stats" id="publishWalkStats"></div>
            <div class="form-group">
              <textarea class="form-input" id="publishWalkText" rows="2" placeholder="附一句话（可选）"></textarea>
            </div>
          </div>
          <div class="modal-footer">
            <button class="btn-confirm" id="btnPublishWalkConfirm" style="width:100%;">发布</button>
          </div>
        </div>
      </div>
```

- [ ] **Step 5: 样式（`demo/style.css` 追加）**

```css
/* ===== Task 7: 崽崽战报/手账 ===== */
.summary-body { padding: 0 var(--sp-lg) 30px; }
.summary-map { height: 190px; border-radius: var(--r-card); overflow: hidden; box-shadow: var(--shadow-sm); background: var(--c-primary-light); }
.summary-stats { display: flex; gap: var(--sp-sm); margin-top: var(--sp-md); }
.summary-stat {
  flex: 1; background: var(--c-surface); border-radius: var(--r-card);
  box-shadow: var(--shadow-sm); padding: 14px 8px; text-align: center;
}
.summary-stat-num { font-size: 20px; font-weight: 700; color: var(--c-primary); font-family: var(--font-en); }
.summary-stat-label { font-size: var(--fs-micro); color: var(--c-text-secondary); margin-top: 2px; }
.summary-dogs { display: flex; gap: 8px; flex-wrap: wrap; margin-top: var(--sp-md); }
.summary-dog {
  background: var(--c-peach-light); color: var(--c-text);
  font-size: var(--fs-caption); font-weight: 600;
  padding: 5px 12px; border-radius: var(--r-pill);
}
.summary-actions { display: flex; gap: 12px; margin-top: var(--sp-lg); }
.summary-actions button { flex: 1; }
.btn-confirm.disabled, .btn-confirm:disabled { opacity: 0.5; }
.publish-walk-stats {
  display: flex; gap: 12px; justify-content: center; flex-wrap: wrap;
  background: var(--c-primary-light); border-radius: var(--r-card);
  padding: 12px; font-size: var(--fs-caption); color: var(--c-primary); font-weight: 600;
}
.records-list { padding: var(--sp-md) var(--sp-lg) 30px; display: flex; flex-direction: column; gap: var(--sp-md); }
.record-card {
  background: var(--c-surface); border-radius: var(--r-card);
  box-shadow: var(--shadow-sm); padding: 14px 16px;
}
.record-card:active { transform: scale(0.98); }
.record-card-top { display: flex; justify-content: space-between; align-items: center; margin-bottom: 6px; }
.record-date { font-size: var(--fs-caption); color: var(--c-text-tertiary); }
.record-published {
  font-size: var(--fs-micro); color: var(--c-peach); background: var(--c-peach-light);
  padding: 2px 8px; border-radius: var(--r-pill); font-weight: 600;
}
.record-stats { display: flex; gap: 12px; font-size: var(--fs-body); color: var(--c-text); font-weight: 600; }
.record-dogs { margin-top: 8px; display: flex; gap: 6px; flex-wrap: wrap; }
```

- [ ] **Step 6: 验证（绿）**

`?v=8`：遛弯页选狗开始遛狗 → 地图遛 15 秒以上（轨迹点会累积）→ 结束遛狗 → 确认 → 战报页：轨迹线可见（绿起点/粉终点）、时长/距离/大卡有值（大卡≈体重×距离km×1.2）、狗chip 正确；「晒到崽崽圈」弹窗发布后按钮变「已晒到崽崽圈 ✓」禁用；「返回地图」正常；崽崽家 → 崽崽手账：该记录在列表，点卡片回看战报（published 状态保留）；再次遛狗结束后手账 +1；遛狗中切 Tab 再回来，轨迹仍继续累积（结束看轨迹点数）。

- [ ] **Step 7: 提交**

```powershell
$env:GIT_AUTHOR_NAME='cpking0715'; $env:GIT_AUTHOR_EMAIL='cpking0715@users.noreply.github.com'; $env:GIT_COMMITTER_NAME='cpking0715'; $env:GIT_COMMITTER_EMAIL='cpking0715@users.noreply.github.com'; git add demo/; git commit -m "feat: 崽崽战报（轨迹地图+统计+一键晒圈）与崽崽手账，遛狗轨迹采样"
```

---

### Task 8: 崽崽圈（时间线/拍爪/晒崽崽发布）

**Files:**
- Modify: `demo/app.js`（renderCircleTimeline/拍爪/发布弹窗）
- Modify: `demo/style.css`（动态卡片/拍爪动画）

- [ ] **Step 1: 时间线与拍爪（`demo/app.js` 追加）**

```js
// ===== 崽崽圈 =====
let publishCoverIdx = 0;
let publishDogId = null;

function visiblePosts() {
  const friendIds = myFriends.map(f => f.id);
  return circlePosts
    .filter(p => p.authorId === 'me' || friendIds.includes(p.authorId))
    .sort((a, b) => circlePosts.indexOf(a) - circlePosts.indexOf(b));
}

function renderCircleTimeline() {
  const tl = document.getElementById('circleTimeline');
  const posts = visiblePosts();
  if (!posts.length) {
    tl.innerHTML = `
      <div class="circle-empty">
        <div class="circle-empty-icon">🐶</div>
        <div class="circle-empty-text">还没有崽崽动态，去碰个爪交个玩伴吧～</div>
        <button class="btn-confirm" id="btnBumpFromCircle">碰个爪 🐾</button>
      </div>`;
    document.getElementById('btnBumpFromCircle').addEventListener('click', () => openBumpOverlay());
    return;
  }

  tl.innerHTML = posts.map(p => `
    <div class="post-card" data-post-id="${p.id}">
      <div class="post-header">
        <div class="post-avatar">${p.authorAvatar}</div>
        <div class="post-author-info">
          <div class="post-author">${p.authorId === 'me' ? '我家崽崽' : p.authorName}</div>
          <div class="post-time">${p.time}</div>
        </div>
      </div>
      ${p.type === 'walkReport' ? `
        <div class="post-walk-report">
          <div class="post-walk-title">🏅 崽崽战报</div>
          <div class="post-walk-stats">
            <span>⏱ ${formatDuration(p.stats.duration)}</span>
            <span>📍 ${formatDist(p.stats.distance)}</span>
            <span>🔥 ${p.stats.calories} 大卡</span>
          </div>
        </div>
      ` : `
        <div class="post-cover" style="background:${p.cover.gradient}">${p.cover.emoji || p.dogEmoji}</div>
      `}
      <div class="post-text">${p.text}</div>
      <div class="post-footer">
        <span class="post-dog-tag">${p.dogEmoji} ${p.dogName}</span>
        <button class="post-like ${p.likedByMe ? 'liked' : ''}" data-like>
          <span class="like-paw">🐾</span> 拍爪 <span class="like-count">${p.likes}</span>
        </button>
      </div>
    </div>
  `).join('');

  tl.querySelectorAll('.post-card').forEach(card => {
    card.querySelector('[data-like]').addEventListener('click', (e) => {
      const p = circlePosts.find(x => x.id === card.dataset.postId);
      if (!p) return;
      p.likedByMe = !p.likedByMe;
      p.likes += p.likedByMe ? 1 : -1;
      const btn = e.currentTarget;
      btn.classList.toggle('liked', p.likedByMe);
      btn.querySelector('.like-count').textContent = p.likes;
      if (p.likedByMe) {
        const burst = document.createElement('span');
        burst.className = 'like-burst';
        burst.textContent = '🐾';
        btn.appendChild(burst);
        setTimeout(() => burst.remove(), 700);
      }
    });
  });
}
```

- [ ] **Step 2: 晒崽崽发布（`demo/app.js` 追加）**

```js
function initCirclePublish() {
  document.getElementById('btnPublishPost').addEventListener('click', () => {
    // 选崽崽
    document.getElementById('publishDogRow').innerHTML = dogsData.map(d => `
      <div class="publish-dog-option" data-dog-id="${d.id}">
        <div class="publish-dog-avatar">${d.emoji}</div>
        <div class="publish-dog-name">${d.name}</div>
      </div>`).join('');
    publishDogId = dogsData.length ? dogsData[0].id : null;
    document.querySelectorAll('#publishDogRow .publish-dog-option').forEach(opt => {
      opt.classList.toggle('selected', parseInt(opt.dataset.dogId) === publishDogId);
      opt.addEventListener('click', () => {
        publishDogId = parseInt(opt.dataset.dogId);
        document.querySelectorAll('#publishDogRow .publish-dog-option').forEach(o =>
          o.classList.toggle('selected', parseInt(o.dataset.dogId) === publishDogId));
      });
    });
    // 选封面
    document.getElementById('publishCoverRow').innerHTML = COVER_PRESETS.map((c, i) => `
      <div class="publish-cover-option ${i === 0 ? 'selected' : ''}" data-cover-idx="${i}" style="background:${c.gradient}">${c.emoji}</div>`).join('');
    publishCoverIdx = 0;
    document.querySelectorAll('#publishCoverRow .publish-cover-option').forEach(opt => {
      opt.addEventListener('click', () => {
        publishCoverIdx = parseInt(opt.dataset.coverIdx);
        document.querySelectorAll('#publishCoverRow .publish-cover-option').forEach(o =>
          o.classList.toggle('selected', parseInt(o.dataset.coverIdx) === publishCoverIdx));
      });
    });
    document.getElementById('publishText').value = '';
    openModal('publishPostModal');
  });

  document.getElementById('btnPublishConfirm').addEventListener('click', () => {
    if (!publishDogId) { alert('先选一只崽崽吧～'); return; }
    const dog = dogsData.find(d => d.id === publishDogId);
    const cover = COVER_PRESETS[publishCoverIdx];
    const text = document.getElementById('publishText').value.trim() || '晒晒我家崽崽～';
    circlePosts.unshift({
      id: 'post' + Date.now(), authorId: 'me', authorName: '我', authorAvatar: '🏠',
      dogName: dog.name, dogEmoji: dog.emoji,
      cover: { gradient: cover.gradient, emoji: cover.emoji },
      text, time: '刚刚', likes: 0, likedByMe: false, type: 'normal',
    });
    closeModal('publishPostModal');
    renderCircleTimeline();
  });
}
```

`DOMContentLoaded` 追加 `initCirclePublish();`。

- [ ] **Step 3: 样式（`demo/style.css` 追加）**

```css
/* ===== Task 8: 崽崽圈 ===== */
.circle-timeline { padding: var(--sp-md) var(--sp-lg) 30px; display: flex; flex-direction: column; gap: var(--sp-md); }
.circle-empty { text-align: center; padding: 60px 24px; }
.circle-empty-icon { font-size: 44px; }
.circle-empty-text { color: var(--c-text-secondary); font-size: var(--fs-body); margin: 12px 0 20px; }
.post-card { background: var(--c-surface); border-radius: var(--r-card); box-shadow: var(--shadow-sm); overflow: hidden; }
.post-header { display: flex; gap: 10px; align-items: center; padding: 12px 14px 8px; }
.post-avatar {
  width: 38px; height: 38px; border-radius: 50%; background: var(--c-primary-light);
  display: flex; align-items: center; justify-content: center; font-size: 19px;
}
.post-author { font-size: var(--fs-body); font-weight: 600; color: var(--c-text); }
.post-time { font-size: var(--fs-micro); color: var(--c-text-tertiary); }
.post-cover {
  height: 150px; display: flex; align-items: center; justify-content: center;
  font-size: 60px; margin: 0 14px; border-radius: var(--r-tag);
}
.post-walk-report {
  margin: 0 14px; padding: 12px 14px; border-radius: var(--r-tag);
  background: linear-gradient(135deg, var(--c-primary-light) 0%, #F4EEF7 100%);
  border: 1px dashed rgba(107,163,190,0.4);
}
.post-walk-title { font-size: var(--fs-caption); font-weight: 700; color: var(--c-primary); margin-bottom: 6px; }
.post-walk-stats { display: flex; gap: 12px; font-size: var(--fs-caption); color: var(--c-text); font-weight: 600; flex-wrap: wrap; }
.post-text { padding: 10px 14px 0; font-size: var(--fs-body); color: var(--c-text); line-height: var(--lh-body); }
.post-footer {
  display: flex; justify-content: space-between; align-items: center;
  padding: 10px 14px 12px;
}
.post-dog-tag {
  font-size: var(--fs-micro); color: var(--c-text-secondary);
  background: var(--c-bg); padding: 3px 10px; border-radius: var(--r-pill);
}
.post-like {
  position: relative; border: none; background: var(--c-bg);
  border-radius: var(--r-pill); padding: 5px 12px;
  font-size: var(--fs-caption); color: var(--c-text-secondary); font-weight: 600;
}
.post-like.liked { background: var(--c-peach-light); color: var(--c-peach); }
.like-burst {
  position: absolute; top: -10px; right: 6px; font-size: 16px;
  animation: likeBurst 0.7s ease-out forwards; pointer-events: none;
}
@keyframes likeBurst { from { transform: translateY(0) scale(0.6); opacity: 1; } to { transform: translateY(-18px) scale(1.3); opacity: 0; } }
/* 晒崽崽弹窗 */
.publish-dog-row { display: flex; gap: 10px; flex-wrap: wrap; }
.publish-dog-option { text-align: center; cursor: pointer; }
.publish-dog-avatar {
  width: 48px; height: 48px; border-radius: 50%; background: var(--c-bg);
  display: flex; align-items: center; justify-content: center; font-size: 24px;
  border: 2px solid transparent;
}
.publish-dog-option.selected .publish-dog-avatar { border-color: var(--c-primary); background: var(--c-primary-light); }
.publish-dog-name { font-size: var(--fs-micro); color: var(--c-text-secondary); margin-top: 4px; }
.publish-cover-row { display: flex; gap: 10px; flex-wrap: wrap; }
.publish-cover-option {
  width: 52px; height: 52px; border-radius: var(--r-tag);
  display: flex; align-items: center; justify-content: center; font-size: 22px;
  border: 2.5px solid transparent; cursor: pointer;
}
.publish-cover-option.selected { border-color: var(--c-text); }
```

- [ ] **Step 4: 验证（绿）**

`?v=9`：初始崽崽圈为空状态（无玩伴）+碰个爪按钮；碰个爪加奶茶妈妈 → 切崽崽圈：她的 2 条预置动态出现（渐变封面+文案+拍爪）；拍爪 → 爪印飞出动画、计数 +1、按钮变蜜桃粉，再拍取消；战报动态（Task 7 晒圈后）显示专属模板（无封面图，战报卡片样式）；「晒崽崽」：选狗/选封面/输文案发布 → 新动态置顶（我家崽崽/刚刚）；未选狗时发布提示（需先手动取消默认选中验证 alert）；删除玩伴后其动态从时间线消失。

- [ ] **Step 5: 提交**

```powershell
$env:GIT_AUTHOR_NAME='cpking0715'; $env:GIT_AUTHOR_EMAIL='cpking0715@users.noreply.github.com'; $env:GIT_COMMITTER_NAME='cpking0715'; $env:GIT_COMMITTER_EMAIL='cpking0715@users.noreply.github.com'; git add demo/; git commit -m "feat: 崽崽圈（仅玩伴可见时间线 + 拍爪动画 + 晒崽崽发布）"
```

---

### Task 9: E2E 测试（TC-31+）与回归

**Files:**
- Modify: `tests/test-cases.md`（追加模块 F~L 用例）
- Modify: `tests/test-results.md`（执行结果）

- [ ] **Step 1: 编写测试用例（`tests/test-cases.md` 追加）**

自 TC-31 编号，覆盖设计文档第 7 节全部项：
- **模块 F 碰个爪**：TC-31 四阶段动画完整（靠近→雷达→发现→成功）；TC-32 添加成功联动（玩伴 badge/列表/地图标记）；TC-33 候选耗尽空状态（碰完 4 位后再碰）；TC-34 陌生汪简卡引导定向碰爪
- **模块 G 玩伴管理**：TC-35 单独开关「让 ta 看见我家崽崽」切换保持；TC-36 删除玩伴（确认弹窗+列表/崽崽圈/地图标记联动）；TC-37 ta 家崽崽档案完整（品种/毛色/脾气/喜好/疫苗）；TC-38 遛弯状态显示（绿点脉冲/灰点）
- **模块 H 崽崽在哪**：TC-39 默认仅玩伴选中；TC-40 三级切换生效；TC-41 关闭行踪提示条；TC-42 单独管理跳玩伴列表
- **模块 I 地图玩伴标记**：TC-43 玩伴崽崽弹跳+光环渲染；TC-44 位置缓慢移动；TC-45 点击弹档案卡带玩伴徽章；TC-46 陌生汪普通标记无特效
- **模块 J 崽崽战报**：TC-47 结束后战报页轨迹渲染；TC-48 时长/距离/大卡正确（大卡≈体重×km×1.2）；TC-49 一键晒圈（动态出现+按钮禁用）；TC-50 写入崽崽手账（列表+1，回看战报）；TC-51 遛狗中切 Tab 轨迹不中断
- **模块 K 崽崽圈**：TC-52 仅玩伴动态可见（加玩伴前后对比）；TC-53 拍爪动画与计数±1；TC-54 晒崽崽发布置顶；TC-55 战报动态模板渲染
- **模块 L 撒欢地**：TC-56 搜索实时过滤；TC-57 无结果空状态；TC-58 宝藏角标/理由/热度展示；TC-59 点卡片定位地图 flyTo+弹卡
- **回归**：TC-60 原 TC-01~30 重点回归（遛狗生命周期/Tab 切换/多狗遛狗/狗狗档案 CRUD）+ 全流程无 JS 错误

每条用例含：前置条件 / 操作步骤 / 预期结果。

- [ ] **Step 2: 启动环境**

```powershell
python -m http.server 8090 --directory demo
```

（后台运行；若端口占用先杀旧进程。浏览器用 Chrome DevTools MCP，视口 390x844，URL 带 `?v=10` 起递增破缓存。）

- [ ] **Step 3: 逐条执行 TC-31~TC-59（Chrome DevTools MCP）**

要点：
- 优先真实点击（take_snapshot 拿 uid → click），脚本只做状态检查（evaluate_script 用普通箭头函数，不能 IIFE/顶层 await）
- 每执行一批用例后 `list_console_messages` 确认无 JS 错误
- 碰个爪流程需等待动画时序（靠近 0.9s → 雷达 2.6s），用 wait_for 或延时快照
- 发现缺陷 → 修复 → 重测该用例并补回归

- [ ] **Step 4: 回归原 TC-01~30 重点项（TC-60）**

重点：热力图渲染、筛选、遛狗开始/结束生命周期（现结束会跳战报页，验证返回地图后状态干净）、多狗遛狗标记、狗狗档案编辑保存（breed 不丢）、5 Tab 切换无异常。

- [ ] **Step 5: 更新 `tests/test-results.md`**

汇总：总用例数、通过数、按模块统计表、缺陷发现与修复记录（若有）、结论。

- [ ] **Step 6: 提交**

```powershell
$env:GIT_AUTHOR_NAME='cpking0715'; $env:GIT_AUTHOR_EMAIL='cpking0715@users.noreply.github.com'; $env:GIT_COMMITTER_NAME='cpking0715'; $env:GIT_COMMITTER_EMAIL='cpking0715@users.noreply.github.com'; git add tests/ demo/; git commit -m "test: 崽崽社交体系 E2E 用例 TC-31~TC-60 与执行结果"
```

---

## 自审记录

**Spec 覆盖检查**：
- 碰个爪（5.1）→ Task 3 ✓（含候选耗尽/定向碰爪）
- 玩伴管理（5.2）→ Task 4 ✓（开关/状态/删除/档案）
- 定位权限（5.3）→ Task 5 ✓
- 地图玩伴标记（5.4）→ Task 6 ✓（弹跳/光环/移动/分层/简卡引导）
- 崽崽战报/手账（5.5）→ Task 7 ✓（轨迹/大卡公式/晒圈/手账回看）
- 撒欢地增强（5.6）→ Task 2 ✓（搜索/宝藏角标/无独立推荐区）
- 崽崽圈（5.7）→ Task 8 ✓（仅玩伴可见/拍爪/晒崽崽/战报模板）
- 导航与命名（4.1/3）→ Task 1 ✓（5 Tab/崽崽系全量命名）
- 数据模型（4.2）→ Task 2 ✓（含可见性逻辑）
- 边界处理（第 6 节）→ 分散在 Task 3（候选耗尽）、Task 4（删除联动）、Task 7（切 Tab 不中断）、Task 2（搜索空）、Task 8（空状态/未选崽崽）✓
- 测试（第 7 节）→ Task 9 ✓

**类型一致性检查**：`myFriends`/`dogFriendsPool`/`circlePosts`/`walkRecords`/`spotsData` 字段在各 Task 间一致；`renderFriendMarkers/renderCircleTimeline/openBumpOverlay/openFriendDetail/renderFriendsList/renderWalkRecords/renderSettingsPrivacy/openSubPage/closeSubPage` 等函数名跨任务引用一致。

---

## 完成标准

全部 9 个 Task 验证通过并提交后，按 superpowers:finishing-a-development-branch skill 收尾（本仓库直接在 master 开发，收尾时确认推送远程 `git@github.com:cpking0715/PawTrace.git`）。
