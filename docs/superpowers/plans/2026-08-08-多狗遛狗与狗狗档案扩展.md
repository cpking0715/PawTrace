# 多狗遛狗与狗狗档案扩展 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 支持同时遛多只狗（地图多标记、可点击查看档案），并扩展狗狗档案字段（毛色/体重/喜好/脾气）。

**Architecture:** 在现有单狗遛狗流程上做最小扩展——`walkState` 单狗字段改为 `dogs[]` 摘要数组，`dogMarker` 改为 `dogMarkers[]` 数组，每只狗独立标记与独立移动；新增底部狗信息卡（复用 spotCard 样式体系）与编辑表单新字段（复用 btn-group/tag-cloud 交互）。

**Tech Stack:** 原生 HTML/CSS/JS + Leaflet 1.9.4；验证方式为真实浏览器 E2E（chrome-devtools MCP + http.server）。

**git 说明：** 仓库 `origin=https://github.com/cpking0715/PawTrace.git`，本机无全局身份，所有提交使用：
`git -C d:\MyProjects\PawTrace -c user.name="cpking0715" -c user.email="cpking0715@users.noreply.github.com" commit -m "..."`

---

### Task 1: 数据模型扩展（dogsData 新增字段）

**Files:**
- Modify: `demo/app.js:25-28`

- [ ] **Step 1: 修改 dogsData，为两只默认狗补充 color/weight/likes**

```js
let dogsData = [
  { id: 1, name: '豆豆', emoji: '🐕', breed: '柯基', size: '小型', age: '青年', color: '棕色', weight: 9.5, likes: ['零食', '奔跑'], tags: ['好动爱玩', '温顺'], vaccine: '已接种' },
  { id: 2, name: '旺财', emoji: '🐩', breed: '金毛', size: '大型', age: '青年', color: '黄色', weight: 28, likes: ['游泳', '社交'], tags: ['社会化良好', '活泼'], vaccine: '已接种' },
];
```

- [ ] **Step 2: 浏览器加载确认无语法错误**

Run: 打开 `http://localhost:8090/demo/`，Console 无报错，遛狗页两只狗卡正常显示。

- [ ] **Step 3: Commit**

```bash
git -C d:\MyProjects\PawTrace -c user.name="cpking0715" -c user.email="cpking0715@users.noreply.github.com" commit -am "feat: dogsData 扩展毛色/体重/喜好字段"
```

---

### Task 2: 编辑表单 HTML 扩展

**Files:**
- Modify: `demo/index.html:303-313`（脾气改名扩充）
- Modify: `demo/index.html:313` 之后（新增毛色/喜好/体重三个 form-group）

- [ ] **Step 1: 「性格标签」→「脾气」并扩充选项**

替换 `demo/index.html` L303-313 整块：

```html
<div class="form-group">
  <label class="form-label">脾气</label>
  <div class="tag-cloud" id="editTags">
    <button class="tag-item" data-tag="好动爱玩">好动爱玩</button>
    <button class="tag-item" data-tag="胆小怕生">胆小怕生</button>
    <button class="tag-item" data-tag="温顺">温顺</button>
    <button class="tag-item" data-tag="活泼">活泼</button>
    <button class="tag-item" data-tag="粘人">粘人</button>
    <button class="tag-item" data-tag="高冷">高冷</button>
    <button class="tag-item" data-tag="不喜欢大狗">不喜欢大狗</button>
    <button class="tag-item" data-tag="不喜欢小狗">不喜欢小狗</button>
    <button class="tag-item" data-tag="会护食">会护食</button>
    <button class="tag-item" data-tag="社会化良好">社会化良好</button>
  </div>
</div>
```

- [ ] **Step 2: 在「脾气」与「疫苗状态」之间插入 毛色/喜好/体重 三个区块**

在替换后的脾气 form-group 之后、`<!-- 疫苗状态 -->` form-group 之前插入：

```html
<div class="form-group">
  <label class="form-label">毛色</label>
  <div class="btn-group wrap" id="editColor">
    <button class="btn-option color-option" data-val="黄色"><span class="color-dot" style="background:#F7C873"></span>黄色</button>
    <button class="btn-option color-option" data-val="白色"><span class="color-dot" style="background:#F1EFEA"></span>白色</button>
    <button class="btn-option color-option" data-val="黑色"><span class="color-dot" style="background:#4A4A4A"></span>黑色</button>
    <button class="btn-option color-option" data-val="棕色"><span class="color-dot" style="background:#A5713F"></span>棕色</button>
    <button class="btn-option color-option" data-val="花色"><span class="color-dot" style="background:conic-gradient(#fff 0 25%, #4A4A4A 0 50%, #fff 0 75%, #4A4A4A 0)"></span>花色</button>
    <button class="btn-option color-option" data-val="灰色"><span class="color-dot" style="background:#B8B8B8"></span>灰色</button>
  </div>
</div>
<div class="form-group">
  <label class="form-label">喜好</label>
  <div class="tag-cloud" id="editLikes">
    <button class="tag-item" data-tag="零食">零食</button>
    <button class="tag-item" data-tag="玩具">玩具</button>
    <button class="tag-item" data-tag="游泳">游泳</button>
    <button class="tag-item" data-tag="奔跑">奔跑</button>
    <button class="tag-item" data-tag="社交">社交</button>
    <button class="tag-item" data-tag="安静">安静</button>
  </div>
</div>
<div class="form-group">
  <label class="form-label">体重</label>
  <div class="weight-input-wrap">
    <input type="number" class="form-input" id="editWeight" placeholder="如 8.5" min="0" step="0.1" />
    <span class="weight-unit">kg</span>
  </div>
</div>
```

- [ ] **Step 3: Commit**

```bash
git -C d:\MyProjects\PawTrace -c user.name="cpking0715" -c user.email="cpking0715@users.noreply.github.com" commit -am "feat: 编辑表单新增毛色/喜好/体重，脾气标签扩充"
```

---

### Task 3: 编辑表单新控件样式

**Files:**
- Modify: `demo/style.css`（在 `.btn-option.active` 块之后追加）

- [ ] **Step 1: 追加毛色色板与体重输入样式**

在 `demo/style.css` L783（`.btn-option.active` 块结束）之后插入：

```css
.btn-group.wrap { flex-wrap: wrap; }
.color-option {
  flex: 0 1 auto;
  min-width: 88px;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  padding: 9px 10px;
}
.color-dot {
  width: 14px;
  height: 14px;
  border-radius: 50%;
  border: 1px solid rgba(0,0,0,0.10);
  display: inline-block;
  flex-shrink: 0;
}
.weight-input-wrap { position: relative; }
.weight-input-wrap .form-input { padding-right: 44px; }
.weight-unit {
  position: absolute;
  right: 14px;
  top: 50%;
  transform: translateY(-50%);
  font-size: 13px;
  color: var(--c-text-tertiary);
  pointer-events: none;
}
```

- [ ] **Step 2: 浏览器目检**

Run: 打开编辑页（我的 → 我的狗狗 → 点豆豆卡片），确认毛色 6 色块换行排列、色点显示正确、体重输入框带 kg 后缀。

- [ ] **Step 3: Commit**

```bash
git -C d:\MyProjects\PawTrace -c user.name="cpking0715" -c user.email="cpking0715@users.noreply.github.com" commit -am "style: 毛色色板与体重输入样式"
```

---

### Task 4: 编辑表单 JS（交互/回填/保存）

**Files:**
- Modify: `demo/app.js:687-710`（initDogEditPage 内新增两个事件绑定）
- Modify: `demo/app.js:719-736`（保存逻辑读取新字段）
- Modify: `demo/app.js:755-758`（openDogEdit 重置新控件）
- Modify: `demo/app.js:776-781`（openDogEdit 回填新字段）
- Modify: `demo/app.js:785-788`（新增模式默认值）

- [ ] **Step 1: initDogEditPage 增加 毛色单选 / 喜好多选 事件绑定**

在 L710（`#editTags .tag-item` 绑定块）之后插入：

```js
document.querySelectorAll('#editColor .btn-option').forEach(btn => {
  btn.addEventListener('click', () => {
    document.querySelectorAll('#editColor .btn-option').forEach(b => b.classList.remove('active'));
    btn.classList.add('active');
  });
});

document.querySelectorAll('#editLikes .tag-item').forEach(btn => {
  btn.addEventListener('click', () => btn.classList.toggle('active'));
});
```

- [ ] **Step 2: 保存逻辑读取新字段**

在 L724（`activeTags` 行）之后、L725（`emoji` 行）之前插入：

```js
const colorBtn = document.querySelector('#editColor .btn-option.active');
const weightVal = document.getElementById('editWeight').value.trim();
const activeLikes = [...document.querySelectorAll('#editLikes .tag-item.active')].map(b => b.dataset.tag);
```

在 L735（`vaccine` 行）之后、L737（`};`）之前插入：

```js
color: colorBtn ? colorBtn.dataset.val : '黄色',
weight: weightVal === '' ? null : (parseFloat(weightVal) || null),
likes: activeLikes,
```

- [ ] **Step 3: openDogEdit 重置新控件**

L756 改为：

```js
document.querySelectorAll('#editTags .tag-item, #editColor .btn-option, #editLikes .tag-item').forEach(b => b.classList.remove('active'));
document.getElementById('editName').value = '';
document.getElementById('editWeight').value = '';
```

- [ ] **Step 4: openDogEdit 编辑回填**

在 L780（`dog.tags.forEach` 块结束）之后插入：

```js
const colorBtn = document.querySelector(`#editColor .btn-option[data-val="${dog.color}"]`);
if (colorBtn) colorBtn.classList.add('active');
if (dog.weight) document.getElementById('editWeight').value = dog.weight;
(dog.likes || []).forEach(like => {
  const likeBtn = document.querySelector(`#editLikes .tag-item[data-tag="${like}"]`);
  if (likeBtn) likeBtn.classList.add('active');
});
```

- [ ] **Step 5: 新增模式默认毛色=黄色**

L787（`editVaccine` 默认行）之后插入：

```js
document.querySelector('#editColor .btn-option[data-val="黄色"]').classList.add('active');
```

- [ ] **Step 6: 浏览器验证 TC-30 前置**

Run: 编辑豆豆 → 改毛色=黑色、体重=12.5、喜好选「玩具」「奔跑」→ 保存 → 重开编辑页，确认回填一致（黑色 active、12.5、玩具/奔跑 active）；添加新狗 → 确认毛色默认黄色、体重为空、喜好全不选。

- [ ] **Step 7: Commit**

```bash
git -C d:\MyProjects\PawTrace -c user.name="cpking0715" -c user.email="cpking0715@users.noreply.github.com" commit -am "feat: 编辑表单新字段读写与回填"
```

---

### Task 5: 狗信息卡 HTML + CSS

**Files:**
- Modify: `demo/index.html`（在 L104 `</div>` spotCard 结束之后、pageMap 结束之前插入）
- Modify: `demo/style.css`（在 L1264 `.dog-map-name` 块之后追加）

- [ ] **Step 1: index.html 插入狗信息卡 DOM**

在 `</div>`（spotCard 结束，L104）之后插入：

```html
<!-- 狗狗信息卡片 -->
<div class="spot-card dog-info-card" id="dogInfoCard">
  <div class="spot-card-handle"></div>
  <div class="dog-info-body">
    <div class="dog-info-head">
      <div class="dog-info-avatar" id="dogInfoAvatar">🐕</div>
      <div class="dog-info-head-right">
        <div class="dog-info-name" id="dogInfoName">豆豆</div>
        <div class="dog-info-breed" id="dogInfoBreed">柯基 · 小型犬 · 青年 · 9.5kg</div>
      </div>
      <button class="dog-info-edit-btn" id="btnDogInfoEdit">编辑档案</button>
    </div>
    <div class="dog-info-row" id="dogInfoColorRow">
      <span class="dog-info-label">毛色</span>
      <span class="dog-info-color"><span class="dog-info-color-dot" id="dogInfoColorDot"></span><span id="dogInfoColorText">黄色</span></span>
    </div>
    <div class="dog-info-row" id="dogInfoTagsRow">
      <span class="dog-info-label">脾气</span>
      <span class="dog-info-tags" id="dogInfoTags"></span>
    </div>
    <div class="dog-info-row" id="dogInfoLikesRow">
      <span class="dog-info-label">喜好</span>
      <span class="dog-info-tags" id="dogInfoLikes"></span>
    </div>
    <div class="dog-info-row">
      <span class="dog-info-label">疫苗</span>
      <span class="dog-info-vaccine" id="dogInfoVaccine">已接种</span>
    </div>
  </div>
</div>
```

- [ ] **Step 2: style.css 追加狗信息卡样式**

在 L1264（`.dog-map-name` 块结束）之后追加：

```css
/* ===== Dog Info Card ===== */
.dog-info-body { padding: 4px 16px 18px; }
.dog-info-head {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 10px;
}
.dog-info-avatar {
  width: 56px;
  height: 56px;
  border-radius: var(--r-circle);
  background: var(--c-primary-light);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 30px;
  flex-shrink: 0;
  box-shadow: inset 0 0 0 1.5px rgba(107,163,190,0.25);
}
.dog-info-head-right { flex: 1; min-width: 0; }
.dog-info-name { font-size: 18px; font-weight: 700; color: var(--c-text); }
.dog-info-breed { font-size: var(--fs-caption); color: var(--c-text-secondary); margin-top: 2px; }
.dog-info-edit-btn {
  border: 1.5px solid var(--c-primary);
  color: var(--c-primary);
  background: var(--c-surface);
  border-radius: var(--r-pill);
  padding: 6px 12px;
  font-size: 12px;
  font-weight: 600;
  font-family: var(--font-cn);
  cursor: pointer;
  flex-shrink: 0;
  transition: background var(--dur-fast);
}
.dog-info-edit-btn:active { background: var(--c-primary-light); }
.dog-info-row {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  padding: 7px 0;
  border-top: 0.5px solid var(--c-border);
  font-size: 13px;
}
.dog-info-label { width: 40px; flex-shrink: 0; color: var(--c-text-tertiary); padding-top: 2px; }
.dog-info-color { display: flex; align-items: center; gap: 6px; color: var(--c-text); }
.dog-info-color-dot {
  width: 14px;
  height: 14px;
  border-radius: 50%;
  border: 1px solid rgba(0,0,0,0.10);
  flex-shrink: 0;
}
.dog-info-tags { display: flex; flex-wrap: wrap; gap: 6px; }
.dog-info-vaccine { color: var(--c-text); }
```

- [ ] **Step 3: Commit**

```bash
git -C d:\MyProjects\PawTrace -c user.name="cpking0715" -c user.email="cpking0715@users.noreply.github.com" commit -am "feat: 狗信息卡 DOM 与样式"
```

---

### Task 6: 狗信息卡 JS（show/hide + 关闭联动）

**Files:**
- Modify: `demo/app.js:107`（地图 click 同时关闭狗信息卡）
- Modify: `demo/app.js:181-183`（hideSpotCard 附近新增函数）

- [ ] **Step 1: 新增 COLOR_HEX 映射与 showDogCard/hideDogCard**

在 `demo/app.js` `hideSpotCard()` 函数（L181-183）之后插入：

```js
// ===== Dog Info Card =====
const COLOR_HEX = {
  '黄色': '#F7C873', '白色': '#F1EFEA', '黑色': '#4A4A4A', '棕色': '#A5713F',
  '花色': 'conic-gradient(#fff 0 25%, #4A4A4A 0 50%, #fff 0 75%, #4A4A4A 0)',
  '灰色': '#B8B8B8',
};

function showDogCard(dogId) {
  const dog = dogsData.find(d => d.id === dogId);
  if (!dog) return;
  hideSpotCard();

  document.getElementById('dogInfoAvatar').textContent = dog.emoji;
  document.getElementById('dogInfoName').textContent = dog.name;
  document.getElementById('dogInfoBreed').textContent =
    dog.breed + ' · ' + dog.size + '犬 · ' + dog.age + ' · ' + (dog.weight ? dog.weight + 'kg' : '-');
  document.getElementById('dogInfoVaccine').textContent = dog.vaccine;

  const colorRow = document.getElementById('dogInfoColorRow');
  if (dog.color) {
    document.getElementById('dogInfoColorDot').style.background = COLOR_HEX[dog.color] || '#F7C873';
    document.getElementById('dogInfoColorText').textContent = dog.color;
    colorRow.style.display = '';
  } else {
    colorRow.style.display = 'none';
  }

  const tagsRow = document.getElementById('dogInfoTagsRow');
  if (dog.tags && dog.tags.length) {
    document.getElementById('dogInfoTags').innerHTML = dog.tags.map(t => `<span class="dog-tag trait">${t}</span>`).join('');
    tagsRow.style.display = '';
  } else {
    tagsRow.style.display = 'none';
  }

  const likesRow = document.getElementById('dogInfoLikesRow');
  if (dog.likes && dog.likes.length) {
    document.getElementById('dogInfoLikes').innerHTML = dog.likes.map(t => `<span class="dog-tag trait">${t}</span>`).join('');
    likesRow.style.display = '';
  } else {
    likesRow.style.display = 'none';
  }

  document.getElementById('dogInfoCard').classList.add('show');
  document.getElementById('btnDogInfoEdit').onclick = () => { hideDogCard(); openDogEdit(dog.id); };
}

function hideDogCard() {
  document.getElementById('dogInfoCard').classList.remove('show');
}
```

- [ ] **Step 2: 地图空白点击同时关闭狗信息卡**

L107 改为：

```js
map.on('click', function () { hideSpotCard(); hideDogCard(); });
```

- [ ] **Step 3: 浏览器验证**

Run: 地图页点击公园标记 → spotCard 出现；`evaluate_script` 调用 `showDogCard(1)` → 狗信息卡出现且 spotCard 消失；点地图空白 → 卡关闭。

- [ ] **Step 4: Commit**

```bash
git -C d:\MyProjects\PawTrace -c user.name="cpking0715" -c user.email="cpking0715@users.noreply.github.com" commit -am "feat: 狗信息卡显示/关闭逻辑"
```

---

### Task 7: 多狗遛狗（walkState 多狗化 + 多标记 + 状态栏/结束弹窗）

**Files:**
- Modify: `demo/app.js:41-57`（状态结构）
- Modify: `demo/app.js:383-402`（startWalkingPage 收集多狗）
- Modify: `demo/app.js:446-447`（endWalkingPage 清 dogs）
- Modify: `demo/app.js:457-519`（showWalkMarkersOnMap 多标记）
- Modify: `demo/app.js:521-531`（hideWalkMarkers 清数组）
- Modify: `demo/app.js:533-549`（updateWalkStatusBar 多狗）
- Modify: `demo/app.js:552-563`（endWalkFromMap 填充狗列表）
- Modify: `demo/app.js:572-573`（confirmEndWalk 清 dogs）
- Modify: `demo/index.html:74-87`（状态栏 emoji 容器改为内联容器）
- Modify: `demo/index.html:368-378`（结束弹窗加狗列表行）
- Modify: `demo/style.css`（状态栏多 emoji、结束弹窗狗列表样式）

- [ ] **Step 1: 状态结构改造**

L41-57 改为：

```js
// ===== Walk State (global遛狗状态) =====
let walkState = {
  isWalking: false,
  dogs: [],        // [{ id, name, breed, emoji }] 本次遛狗的所有狗（运行期挂 _lat/_lng）
  startLat: 0,
  startLng: 0,
  startTime: null,
};
let walkMapTimer = null;       // 地图遛狗计时器
let walkMoveTimer = null;      // 模拟移动计时器
let userMarker = null;         // 用户位置标记
let dogMarkers = [];           // 狗狗标记数组
let walkSeconds = 0;           // 地图遛狗秒数
let walkDistance = 0;          // 模拟距离(米)
```

- [ ] **Step 2: startWalkingPage 收集全部选中狗**

L390-402 替换为：

```js
  // 收集所有选中的狗
  const selectedDogs = [];
  selected.forEach(card => {
    const dog = dogsData.find(d => d.id === parseInt(card.dataset.dogId));
    if (dog) selectedDogs.push({ id: dog.id, name: dog.name, breed: dog.breed, emoji: dog.emoji });
  });
  if (selectedDogs.length === 0) return;

  // 记录遛狗状态（Demo 无真实定位，「当前位置」固定为城市中心人民公园附近）
  walkState.isWalking = true;
  walkState.dogs = selectedDogs;
  walkState.startLat = 30.62;
  walkState.startLng = 104.06;
  walkState.startTime = new Date();
```

- [ ] **Step 3: endWalkingPage / confirmEndWalk 清空 dogs**

L446-447 处（endWalkingPage）改为：

```js
  // 清除遛狗状态
  walkState.isWalking = false;
  walkState.dogs = [];
  walkState.startTime = null;
```

L572-573 处（confirmEndWalk）改为：

```js
  // 清除遛狗状态
  walkState.isWalking = false;
  walkState.dogs = [];
  walkState.startTime = null;
```

- [ ] **Step 4: showWalkMarkersOnMap 多标记错开排布**

L462-491 替换为：

```js
  // 清除旧标记（如果存在）
  if (userMarker) { map.removeLayer(userMarker); userMarker = null; }
  dogMarkers.forEach(m => { if (m) map.removeLayer(m); });
  dogMarkers = [];

  const startLat = walkState.startLat;
  const startLng = walkState.startLng;

  // 用户位置标记（蓝色脉冲圆点）
  const userIcon = L.divIcon({
    className: '',
    html: '<div class="user-location-marker"><div class="user-location-pulse"></div><div class="user-location-dot"></div></div>',
    iconSize: [40, 40],
    iconAnchor: [20, 20],
  });
  userMarker = L.marker([startLat, startLng], { icon: userIcon, zIndexOffset: 1000 }).addTo(map);

  // 多只狗狗标记：在起点附近错开排布（约 60~90 米间隔），互不重叠、可独立点击
  walkState.dogs.forEach((dog, i) => {
    if (!dog._lat) { dog._lat = startLat + 0.0007 + i * 0.0009; dog._lng = startLng + 0.0005 + i * 0.0007; }
    const dogIcon = L.divIcon({
      className: '',
      html: `<div class="dog-map-marker"><div class="dog-map-icon">${dog.emoji}</div><div class="dog-map-name">${dog.name}</div></div>`,
      iconSize: [50, 56],
      iconAnchor: [25, 28],
    });
    const marker = L.marker([dog._lat, dog._lng], { icon: dogIcon, zIndexOffset: 900 + i }).addTo(map);
    marker.on('click', function (e) {
      L.DomEvent.stopPropagation(e);
      showDogCard(dog.id);
    });
    dogMarkers.push(marker);
  });
```

L513-517 移动模拟替换为：

```js
    // 模拟移动（每4秒每只狗独立小幅移动）
    clearInterval(walkMoveTimer);
    walkMoveTimer = setInterval(() => {
      if (!walkState.isWalking || dogMarkers.length === 0) return;
      walkState.dogs.forEach((dog, i) => {
        if (!dogMarkers[i]) return;
        dog._lat += (Math.random() - 0.4) * 0.0002;
        dog._lng += (Math.random() - 0.4) * 0.0002;
        dogMarkers[i].setLatLng([dog._lat, dog._lng]);
      });
    }, 4000);
```

- [ ] **Step 5: hideWalkMarkers 清数组 + 关闭狗信息卡**

L521-531 改为：

```js
function hideWalkMarkers() {
  if (userMarker) { map.removeLayer(userMarker); userMarker = null; }
  dogMarkers.forEach(m => { if (map && m) map.removeLayer(m); });
  dogMarkers = [];
  clearInterval(walkMapTimer);
  clearInterval(walkMoveTimer);
  walkMapTimer = null;
  walkMoveTimer = null;
  hideDogCard();

  // 隐藏状态栏
  document.getElementById('walkStatusBar').style.display = 'none';
}
```

- [ ] **Step 6: updateWalkStatusBar 多狗 emoji + 全部名字**

L537-538 改为：

```js
    document.getElementById('walkStatusDogEmoji').innerHTML =
      walkState.dogs.map(d => `<span class="walk-status-dog-emoji">${d.emoji}</span>`).join('');
    document.getElementById('walkStatusDogName').textContent =
      walkState.dogs.map(d => d.name).join('、');
```

- [ ] **Step 7: endWalkFromMap 填充狗列表**

L553（`endWalkFromMap` 内第一行）之前插入：

```js
  document.getElementById('endWalkDogs').innerHTML =
    walkState.dogs.map(d => `<span class="end-walk-dog">${d.emoji}</span>`).join('');
```

- [ ] **Step 8: 状态栏多 emoji 样式**

L76 的 `<span class="walk-status-dog-emoji" id="walkStatusDogEmoji">🐕</span>` 保持不变（JS 用 innerHTML 向容器内填充多个 span）。CSS 中：

将 `demo/style.css` L1146-1149 旧规则：

```css
.walk-status-dog-emoji {
  font-size: 22px;
  flex-shrink: 0;
}
```

替换为容器+子项两套规则：

```css
#walkStatusDogEmoji {
  display: flex;
  gap: 2px;
  flex-shrink: 0;
}
.walk-status-dog-emoji {
  font-size: 20px;
  line-height: 1;
  flex-shrink: 0;
}
```

并将 `.walk-status-dog-name`（L1151-1156）追加溢出省略：

```css
.walk-status-dog-name {
  font-size: 14px;
  font-weight: 700;
  color: var(--c-text);
  line-height: 1.2;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
```

（原 `.walk-status-dog-emoji` 单元素规则改为 `#walkStatusDogEmoji` 容器规则：`#walkStatusDogEmoji { display: flex; gap: 2px; flex-shrink: 0; }`，替换 L1146-1149 的旧规则。）

- [ ] **Step 9: 结束弹窗加狗列表**

`demo/index.html` L368-379 的 `.modal-body` 内、`.end-walk-stats` 之前插入：

```html
<div class="end-walk-dogs" id="endWalkDogs"></div>
```

`demo/style.css` `.end-walk-stats`（L1268）之前追加：

```css
.end-walk-dogs {
  display: flex;
  justify-content: center;
  gap: 10px;
  padding: 4px 0 14px;
}
.end-walk-dog {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: var(--c-primary-light);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
  box-shadow: inset 0 0 0 1px rgba(107,163,190,0.20);
}
```

- [ ] **Step 10: 修复遛狗选择卡「小型型」文案**

L343 改为：

```js
<div class="dog-select-desc">${dog.breed} · ${dog.size} · ${dog.tags.join('、')}</div>
```

- [ ] **Step 11: 浏览器回归验证**

Run: 遛狗 Tab 勾选豆豆+旺财 → 开始遛狗 → 地图出现 2 个狗标记（错开排布）、状态栏 2 个 emoji + 「豆豆、旺财」；等待 4s+ 确认两标记各自移动；点击豆豆标记 → 狗信息卡出现；切走再切回地图 → 标记恢复且位置保持；结束遛狗 → 标记/状态栏/信息卡全部清除，弹窗显示 2 个狗 emoji。

- [ ] **Step 12: Commit**

```bash
git -C d:\MyProjects\PawTrace -c user.name="cpking0715" -c user.email="cpking0715@users.noreply.github.com" commit -am "feat: 多狗遛狗（多标记错开排布、状态栏与结束弹窗多狗）"
```

---

### Task 8: E2E 测试用例与结果

**Files:**
- Modify: `tests/test-cases.md`（追加 TC-28/29/30）
- Modify: `tests/test-results.md`（执行后追加结果与汇总 30/30）

- [ ] **Step 1: 追加测试用例 TC-28/29/30**

`tests/test-cases.md` 末尾追加：

```markdown
### TC-28 多狗同时遛狗显示多个独立标记
- 前置：默认两只狗存在；已进入遛狗 Tab
- 步骤：勾选「豆豆」「旺财」→ 点「开始遛狗」→ 自动切到地图
- 预期：地图出现 2 个 `.dog-map-marker`（emoji 🐕 与 🐩，名字「豆豆」「旺财」），位置错开不重叠；状态栏显示 2 个 emoji，名字为「豆豆、旺财」；4 秒后两个标记均发生位置变化

### TC-29 点击狗标记弹出狗信息卡
- 前置：遛狗中（TC-28 状态）
- 步骤：点击「豆豆」标记
- 预期：`#dogInfoCard` 显示，头部为 🐕/豆豆，毛色/体重/脾气/喜好/疫苗行完整（含毛色色点）；点地图空白处 → 卡片关闭；再次点击「旺财」标记 → 显示旺财档案

### TC-30 档案新增字段保存与回填
- 前置：我的 → 我的狗狗
- 步骤：编辑豆豆 → 毛色选「黑色」、体重填 12.5、喜好选「玩具」「奔跑」→ 保存 → 重新进入编辑页
- 预期：毛色「黑色」active、体重 12.5、喜好「玩具」「奔跑」active；遛狗中点击豆豆标记 → 信息卡显示 黑色/12.5kg/玩具、奔跑
```

- [ ] **Step 2: 浏览器执行 TC-28/29/30 + 回归 TC-13/14/15**

Run: 启动 `python -m http.server 8090 --directory d:\MyProjects\PawTrace\demo`（若未运行）；chrome-devtools 打开隔离上下文页面，按 `tests/test-results.md` 既有格式逐条执行并记录。

- [ ] **Step 3: 更新 test-results.md**

追加 TC-28/29/30 结果，汇总 27+3=30/30 通过；新增本轮功能说明。

- [ ] **Step 4: Commit**

```bash
git -C d:\MyProjects\PawTrace -c user.name="cpking0715" -c user.email="cpking0715@users.noreply.github.com" commit -am "test: 多狗遛狗与档案扩展 E2E 用例与结果"
```

---

### Task 9: 最终提交与推送

- [ ] **Step 1: 检查状态并推送**

```bash
git -C d:\MyProjects\PawTrace status --short
git -C d:\MyProjects\PawTrace log --oneline
```

确认无未提交改动；若远程为空仓库则直接推送（不使用 force）：

```bash
git -C d:\MyProjects\PawTrace push -u origin main
```

若远程已有历史导致推送失败：`git pull --rebase origin main` 后重试；rebase 冲突需人工确认，不得使用 `--force`。

- [ ] **Step 2: 交付说明**

向用户汇总：功能点、文件变更、测试结果（30/30）、推送状态。
